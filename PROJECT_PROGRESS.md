# StockTest 项目进度与工作日志

> 这是 StockTest 唯一的滚动进度文档。后续工作直接更新本文件，不再为同一阶段重复创建“进度报告”“明日计划”或相近名称的文档。

## 文档用途

- 新会话启动时先阅读 [PROJECT_CONTEXT.md](./PROJECT_CONTEXT.md)，再阅读本文件最近一条记录。
- 本文件记录已完成事项、验证证据、明确暂停项和下一步入口。
- 需求口径以用户最新确认内容为准；旧截图、旧计划和旧示例不能覆盖最新确认。
- `sources/` 始终只读，任何数据或页面改动都限定在 `StockTest/` 内。

## 最新记录：2026-09-01

### ETF 真实持仓与 MA150 趋势接入

- 新增 `data_pipeline/fetch_holdings.py`，从 StockAnalysis 公开 ETF holdings 页面解析真实前十大持仓，并保存来源 URL、抓取时间、as-of 日期、总持仓数和缺失原因；页面明确标注该聚合页列出的来源为 Finnhub，不把聚合数据包装成发行方直连数据。
- 本次真实刷新覆盖 71 个板块/行业 ETF：71 个成功。IBIT 改用 iShares 官方 holdings CSV（BTC 与现金两行），USO 改用 USCF 官方 holdings API（前十大期货/国债/掉期头寸）；不再把结构性非股票持仓误报为缺失，也不回退显示 SPY 持仓。
- `data/market_snapshot.json` 现已包含 `holdings` 和 `metadata.holdings`；持仓抓取按日缓存，小时行情刷新不会重复请求 71 个页面，超过 24 小时才重新抓取。
- `market_data.py` 新增 SMA150 计算；使用最新已确认日线收盘价与 MA150 比较，写入 `ma150` 和 `trend150`。数据不足时标记“数据不足”，不使用未收盘日线。
- 行业动能榜新增“趋势（MA150）”列；ETF 名称改为直接打开对应 TradingView 图表，前十大按钮仍保留本地详情抽屉。

### 本次需求：交易所链接、指数悬停、Stockbee 动能股票

- TradingView 链接不再把所有代码硬编码为 `AMEX:`：DTCR、QTUM、IBIT、QQQ、SOXX 使用 `NASDAQ:`，ARKK 使用 `CBOE:`，其余默认沿用 `AMEX:`；映射依据 Yahoo `exchangeName` 与发行方/交易所公开资料核对。
- 指数卡片整张卡片均可鼠标悬停触发气泡，气泡继续显示近一个月日线 K 线、价格和 EMA9/EMA21；键盘聚焦也可触发。真实日内快照存在时，四大指数/BTC 卡片继续绘制真实 5M/2H 数据。
- 板块表已删除“收盘价”，第三列改为 MA150 趋势；行业表保持同一列顺序，数字列和状态列不变。
- 初始接入时新增 `data_pipeline/fetch_stockbee_momentum.py` 与 `data/stockbee_momentum.json`，从 Stockbee 50 公开 Google Sheet 生成纯代码名单和来源元数据；当时使用的旧工作表列停留在 `2017-01-03`，页面明确显示“来源已过期，仅供回溯”。该旧口径已在 2026-09-01 切换为用户指定的 `gid=1499398020`。
- 新增“Stockbee 动能股票”独立导航和名单表，仅展示排名与股票代码；代码链接到 TradingView，不混入收盘价或其他指标。刷新流程会同步写入该快照，并保留旧快照作为失败时的可读回退。

### 本次验证

- `test_fetch_stockbee_momentum`、持仓、行情、刷新和 UI 契约共 52 项通过；`app.js` Node 语法检查通过。
- 当时本地浏览器验证：5 张指数卡片；鼠标移入 SPX 后气泡可见并显示“价格 7,711.76”；行业 DTCR 链接为 `NASDAQ:DTCR`；行业前十大按钮打开真实持仓抽屉并显示 10 行；Stockbee 名单显示 50 行并标注旧表日期。

### 当前注意事项

- 该段记录的是旧工作表的历史状态；当前使用用户指定的 `gid=1499398020`，最新日期已更新为 2026-08-25。若未来工作表再次停更超过 30 天，UI 会自动显示来源过期，不能静默替换来源。

### 验证证据

- 41 项数据与 UI 契约测试通过，Python 模块编译通过，前端 JavaScript 语法检查通过。
- 本地页面 `http://127.0.0.1:8765/index.html` 实测：页面非空、行业表显示 15 行、趋势列可见、首个行业 ETF href 为 `https://tw.tradingview.com/chart/e2o5U28E/?symbol=AMEX%3ASPY`、控制台错误/警告为 0。
- 点击“前十大”可打开 SPY 详情抽屉，显示真实来源 `StockAnalysis (source listed: Finnhub)`、日期 `Aug 19, 2026` 及 NVDA/AAPL 等真实持仓。

### 固定行滚动遮挡修复：已完成

- 修复板块/行业表滚动时 SPY 固定行与表头之间的 1px 漏字，以及固定行下方额外空白问题。
- `syncPinnedRowCovers()` 现在读取实际表头高度并写入 `--table-header-height`；固定行使用该变量定位，遮挡层保持 0 高度，仅由固定行自身覆盖被压住的内容。
- 浏览器实测行业表内部滚动：SPY 行紧贴表头、后续行连续显示、无漏字/大空隙；控制台 error/warn 为 0。

### 下一次启动入口

1. 先检查 `data/market_snapshot.json` 的 `metadata.holdings`，确认 24 小时缓存和缺失清单。
2. 浏览器回归验证行业 ETF 外链和非 SPY ETF 持仓抽屉，重点检查 COPX（加拿大上市成分股 sourceTicker）显示是否清楚。
3. 若要提高可靠性，下一阶段应为缺失/低覆盖 ETF 寻找发行方或官方持仓文件，并保留当前 StockAnalysis 作为可审计备用来源。

## 今日记录：2026-08-30

### 本次目标

重构“市场宽度”页面区域，使其更像高密度但可读的研究终端，并解决 Stockbee 20 日原始数据必须滚动到表格底部才能横向查看的问题。

### 已完成

1. **市场宽度图表卡片重构**
   - 将图表标题、说明、当前指标和指标切换器分层排列。
   - 增加“最新观察”读数和最新日期，读数随指标切换同步更新。
   - 保留 Stockbee 原始列切换，不新增新闻热度或基本面展示。
   - 保留浅色/深色主题、克制边框和单一蓝色强调，避免大面积红绿卡片。

2. **Stockbee 原始数据查看体验重构**
   - 20 日原始表默认折叠，展开后在数据卡片顶部提供独立横向滚动条。
   - 日期列保持 sticky 固定，表格纵向区域独立滚动。
   - 横向滚动条与表格视窗双向同步。
   - 支持鼠标拖动，以及 `←`、`→`、`Home`、`End` 键盘操作。
   - 当前表格保持完整结构：20 行、日期 + 15 个指标，共 16 个数据单元格。

3. **响应式细节调整**
   - 小屏幕下“最新观察”改为横向信息行，避免标题和读数挤压。
   - 不改变既有 1440px、1024px、768px 和 390px 的页面结构。

4. **设计技能记录**
   - 已安装并使用 GitHub `leonxlnx/taste-skill` 的审美校准原则；该技能不强行覆盖研究终端规范，仅用于间距、密度、颜色层级和动效克制。
   - Product Design 方向仍为已确认的“盘后研究简报”。

### 后续执行：P0 免费行情源质量核对

本次已开始执行明日启动清单中的 P0，不改动页面组件。

- 新增 `data_pipeline/validate_market_snapshot.py`，把快照质量检查固化为可重复运行的命令。
- 新增 `data_pipeline/test_validate_market_snapshot.py`，先验证覆盖率、供应商日期分组、重复日期和 OHLC 边界异常，再实现校验逻辑。
- 新增 `data/market_snapshot_quality.json`，保存本次对本地行情快照的质量报告。
- 现有行情快照结果：76/76 标的，覆盖率 100%；4,815 根完整 K 线；无缺失标的、重复日期、非法 OHLC 边界、负成交量或最新日期指针错误。
- 当前唯一发现为低风险交易日历差异：Binance BTC 日期范围为 `2026-06-01 → 2026-08-29`，Yahoo 美股日期范围为 `2026-05-29 → 2026-08-27`。这属于供应商日历差异，不应被当作数据损坏；网页需要继续保留日期提示。
- `pendingCount=75` 与快照中的 Yahoo 未收盘候选日线一致；这些记录不参与收盘价、RSI 和多周期收益计算。
- Stockbee 快照核验：20 行、15 个 schema 字段、每行日期 + 15 指标、20 个唯一日期，日期倒序正确，最新日期 `2026-08-28`，来源状态为 `loaded`。

### 日期口径修正：已完成

- `data/market_snapshot.json` 现已为每个标的写入 `calendar`：美股为 `us-equity`，BTC 为 `crypto-24x7`。
- 快照元数据新增 `comparisonDate`、`calendarLatestDates` 和 `calendars`，当前共同比较日为 `2026-08-27`。
- 页面保留 BTC 原生最新日 `2026-08-29`，美股仍以 SPX 的已确认收盘日 `2026-08-27` 为页面基准，不补造周末美股 K 线。
- 质量报告将日历差异从低风险 finding 调整为 `info` note；当前高/中风险 finding 数量为 `0`。
- 页面日期提示明确区分“美股收盘日”和“BTC 最新日”，跨资产比较使用 `comparisonDate`。

可重复运行：

```powershell
<PYTHON> StockTest/data_pipeline/validate_market_snapshot.py StockTest/data/market_snapshot.json --report StockTest/data/market_snapshot_quality.json
```

### P1 半实时刷新状态与备用源评估：已完成

- `refresh_local_data.py` 现在每次尝试都会生成 `data/refresh_status.json`；失败时不覆盖上一份行情与 Stockbee 快照。
- 状态契约覆盖 `ok`、`partial`、`failed`，并保留最近完成时间、最近完整成功时间、分来源状态和错误摘要。
- 网页新增独立刷新状态徽章，90 分钟内显示“数据新鲜”，超过阈值显示“数据过期”，并支持“局部缺失”和“刷新失败 · 保留上次数据”。
- 状态文件缺失不会阻断行情或 Stockbee 快照加载；异步加载顺序也不会覆盖已判断的过期状态。
- 当前实际快照最后成功刷新为 `2026-08-29T17:03:50Z`，本地实测正确显示“数据过期 · 15 小时”。
- 免费备用源评估：
  - Alpaca Basic 覆盖美国股票与 ETF，但免费实时源仅 IEX、需要账号密钥，且不能直接替代四个现货指数；可作为未来 ETF 级备用，不作为当前全量主备切换。
  - Alpha Vantage 免费层为 25 次请求/日，无法支撑 76 个标的一小时轮询；不接入当前半实时流程。
  - CoinGecko Keyless Public API 适合 BTC 低频、非商业教育用途备用，但有 IP 限流；当前 Binance 公开只读 K 线保持主源。
- 本轮没有安装新依赖或插件，避免在没有明确数据授权、密钥和口径收益时增加维护面。

### P2 本地一键常驻运行：已完成

- 新增 `data_pipeline/local_runtime.py`，统一管理网页服务器和每 60 分钟刷新进程。
- 新增 `start-local.cmd`、`status-local.cmd`、`stop-local.cmd`：支持双击启动、状态查看和精确停止。
- 后台进程、PID 和日志只写入 `StockTest/.runtime/`；关闭命令窗口后仍会运行，但不会注册 Windows 计划任务，电脑重启后需要重新启动。
- 重复运行 `start-local.cmd` 会验证进程命令并复用原 PID，不创建第二个网页服务器或刷新循环。
- 停止操作只对 PID 与项目路径、服务命令均匹配的进程生效，避免陈旧 PID 指向其他程序时误停。
- 首次实际运行发现并修复 Windows 批处理尾部反斜杠吞并参数的问题；新增回归测试覆盖精确项目路径。
- 2026-08-30 首次常驻刷新约 90 秒完成：`status=ok`，行情 `76/76`，Stockbee `20` 行；美股最新确认日 `2026-08-28`，BTC 最新日 `2026-08-30`。
- 重复启动实测保持网页 PID `21972`、刷新 PID `16124` 不变；当前两个进程继续在本地运行。
- 页面实测刷新徽章为“数据新鲜 · 16:44”，控制台错误和警告为 `0`。
- 新快照质量结果：`4,890` 根完整 K 线、覆盖率 `100%`、未收盘候选 `0`、finding `0`。

当前判断：P0、P1、P2 均通过；主题 AI 排名仍保持暂停。下一阶段数据工程入口为“美股 ETF 备用源适配”，但候选源需要账号密钥或存在单一交易所口径限制。

### GitHub 版本基线：已完成

- 已安装并验证 GitHub 官方 CLI `2.98.0`，账号为 `bravo-189`。
- 已将 `StockTest/` 初始化为 `main` 分支并创建私有仓库：`https://github.com/bravo-189/StockTest`。
- 基线提交为 `51586daf504b082c6fef6f831646e3422de53263`，提交说明为 `chore: establish StockTest project baseline`。
- `.gitignore` 排除了 `.runtime/`、`.qa-*/`、Python 缓存、日志、环境密钥文件和每小时更新的本地数据快照；动态数据仍保留在本机，不进入 GitHub 历史。
- 上传前大文件检查无超过 10 MB 的待提交文件，敏感模式扫描无命中。
- 基线前重新运行 50 项 Python 测试与 JavaScript 语法检查，全部通过。
- GitHub CLI/API 已验证远程仓库为 `PRIVATE`、默认分支为 `main`，远程提交 SHA 与本地一致。
- 当前 GitHub 连接器尚未列出这个新建私有仓库；后续若要直接用连接器操作 PR/Issue，需要在 GitHub App 设置中授予该仓库访问权限。现有 GitHub CLI 推送不受影响。

### 本次涉及文件

- `index.html`：市场宽度图表标题、最新读数和原始数据滚动控件。
- `styles.css`：图表层级、最新读数、顶部横向滚动条和移动端规则。
- `app.js`：最新值同步、滚动条初始化、滚动同步、ARIA 状态和键盘操作。
- `data_pipeline/test_ui_flow_contract.py`：市场宽度重构契约测试。
- `data_pipeline/refresh_local_data.py`、`data_pipeline/test_refresh_local_data.py`：刷新状态、失败保留和局部缺失测试。
- `data/refresh_status.json`：最近一次本地刷新结果。
- `data_pipeline/local_runtime.py`、`data_pipeline/test_local_runtime.py`：本地服务生命周期与真实进程回归测试。
- `start-local.cmd`、`status-local.cmd`、`stop-local.cmd`：Windows 一键入口。
- `data_pipeline/validate_market_snapshot.py`、`data_pipeline/test_validate_market_snapshot.py`：行情质量检查和回归测试。
- `data/market_snapshot_quality.json`：行情快照质量报告。
- `README.md`、`PROJECT_CONTEXT.md`、`design-qa.md`：同步新的交互说明和验收记录。

### 验证证据

- Python 数据/UI 测试：`50` 项全部通过（刷新状态 3 项、本地运行生命周期与 Windows 路径 2 项）。
- JavaScript 语法检查：通过。
- 本地服务器：`http://127.0.0.1:8765/index.html` 返回 HTTP `200`。
- 浏览器回归：
  - Stockbee 原始表 `20` 行。
  - 首行数据 `16` 个单元格（日期 + 15 指标）。
  - 原始表宽度 `1532px`，视窗宽度 `933px`，横向可滚动范围 `599px`。
  - 顶部滚动条移动到 `360px` 后，表格视窗同步到 `360px`。
  - 最新观察显示 `0.98`，日期为 `2026-08-28`。
  - 刷新状态显示“数据过期 · 15 小时”，类名为 `is-stale`，并保留最后成功时间。
  - 1440、1024、768、390 四个目标宽度均无页面级横向溢出。
  - “全部 60”显示 60 行；板块排序后 SPY 仍为首行；抽屉显示 10 个持仓；Stockbee 展开后保持 20 行。
  - 控制台 error/warn 数量为 `0`。
- `design-qa.md` 已保持 `final result: passed`。

## 当前产品口径（不可回退）

- 网页优先，小程序延期到网页稳定后另立项目。
- 主题 Top 20 当前暂停，不在主动网页展示；候选目录和 AI 契约保留为后续资产。
- 新闻热度和独立基本面 UI 已移除；基本面数据仅可作为未来后台研究输入。
- 主要市场为 SPX、NDX、DJI、RUT 和 BTC，共 5 张卡片；近一个月约 21 个交易日迷你 K 线。
- 板块 ETF 与权重股属于同一页面模块；板块 ETF 共 14 个（含 IBIT），SPY 必须固定在首行。
- 板块表 RSI14、Δ5、Δ20 支持排序，状态使用表头筛选菜单；SPY 行状态不可被改变。
- 板块模块保留 12 个 ETF 权重股入口；详情统一使用右侧抽屉查看前十大持仓。
- 行业表支持“强势 15 / 全部 60”切换、RSI14/Δ5/20 日排序和每行前十大入口，并删除“动能”数据列。
- 市场宽度直接展示 Stockbee 最近半年原始列折线图和可展开原始表，不添加左侧解释卡。
- 浏览器只读取本地 JSON；不把行情 API 或 Stockbee 抓取逻辑放进浏览器。

## 明日启动清单

按以下顺序执行，完成一步再进入下一步：

1. 阅读本文件和 [PROJECT_CONTEXT.md](./PROJECT_CONTEXT.md)，确认没有新的用户决策覆盖当前口径。
2. 检查 `StockTest/sources/` 未被修改；不要重新制作已有页面、截图或数据契约。
3. 运行现有验证：

   ```powershell
   <PYTHON> -m unittest discover -s StockTest/data_pipeline -p 'test_*.py' -v
   <NODE> --check StockTest/app.js
   ```

4. 确认本地页面 `http://127.0.0.1:8765/index.html` 可访问，再检查四个区域：总览、板块与权重股、行业动能、市场宽度。
5. “免费行情源质量核对、半实时刷新异常状态和备用源评估”已于 2026-08-30 完成；不重复运行同一轮检查，除非快照刷新或用户要求复核。
6. 本地一键常驻刷新已完成；后续若无新产品需求，进入需要密钥的美股 ETF 备用源适配决策，主题 AI 排名继续保持暂停。

## 待办优先级

### P0：免费行情源质量核对（已完成：2026-08-30）

- [x] 对 `data/market_snapshot.json` 做结构质量核对，并记录异常，不修改页面组件。
- [x] 确认 Yahoo 未收盘日线仍只进入 `pendingBars`，不混入收盘指标。
- [x] 确认 Stockbee 最新抓取日期、20 行限制和字段完整性。

### P1：半实时刷新与备用源评估（已完成：2026-08-30）

- [x] 完善本地半实时刷新失败提示、数据过期和局部缺失状态。
- [x] 评估免费备用行情源；本轮结论是不新增依赖，美股候选需密钥或存在口径限制。
- [x] 重新执行桌面、平板、窄屏和控制台 QA。

### P2：本地一键常驻运行（已完成：2026-08-30）

- [x] 一键启动网页服务器和每小时刷新进程。
- [x] 重复启动保护、项目进程身份校验和安全停止。
- [x] 项目内 PID/日志记录，不注册系统级任务。
- [x] 完成真实刷新、状态徽章、HTTP 和控制台回归。

### 暂停项

- 20 个主题最终排名和主题网页模块。
- 小程序页面/运行时。
- GitHub Pages 发布。
- 新闻热度和独立基本面展示。

## 防重复工作规则

- 先更新 `PROJECT_PROGRESS.md`，不要新建同类日志文件。
- 先查文件地图和现有测试，再决定是否需要新增文件。
- 页面已有交互不重做：导航、主题切换、排序、状态筛选、SPY 固定、15/60 切换、抽屉和 Stockbee 字段切换均已存在。
- 只在需求直接相关的文件中做精准修改，不顺手重构无关模块。
- 任何“已完成”结论都必须附测试或浏览器证据；没有证据时标记为“待验证”。
- 真实数据、来源、抓取时间和缺失状态必须与数据一起保存；不把示例值描述成实时行情。

## 2026-08-31 长期投资页面入口（本次完成）

- 在现有盘后雷达侧栏新增“长期投资”导航入口，链接到独立 `long-term.html`，不改变原有四个研究区域和任何行情逻辑。
- 新页沿用现有 StockTest 设计系统，提供侧栏、苹果式主题切换、返回盘后雷达按钮和明确的空白占位说明。
- 当前页面不加载行情、Stockbee、主题或投资结论，后续长期投资内容另行确认后再填充。
- 浏览器验证：从 `index.html → 长期投资` 跳转成功；标题为“StockTest · 长期投资”；主题切换可用；返回按钮回到盘后雷达；控制台无错误。

### 下一步入口

长期投资页面暂时保持空白，不重复制作内容。待用户确认长期投资的目标、持仓视图和数据周期后，再进入信息架构与数据契约设计。

## 2026-08-31 市场宽度指标分组（本次完成）

- 将 Stockbee 15 个折线指标拆为“上涨指标”“下跌指标”“其他指标”三个独立区块，避免所有按钮堆叠在同一组中。
- 保留原有字段、默认选中状态、指标切换、最新观察值同步和键盘操作；仅调整语义分组与响应式布局。
- 桌面端三组并列，小屏端自动纵向排列；浏览器验证分组数量为 3、每组 5 个指标，切换“4%上涨”后最新观察值同步更新。
- 控制台无错误；Python 数据与 UI 测试 `59` 项全部通过，JavaScript 语法检查通过。

## 2026-08-31 RS 相对强度接入（本次完成）

### 交付内容

- 板块 ETF 与行业 ETF 表新增 `RS1M`、`RS3M`、`RS6M`、`RS12M` 四个相对强度列，并支持表头排序；SPY 固定首行且四个 RS 值显示为基准 `0.00%`。
- 后台 Yahoo 历史窗口从 3 个月扩展为 2 年，以覆盖 12 个月（约 252 个美股交易日）相对强度计算；当前快照中美股标的最多保留 501 根已确认日线。
- RS 使用与 SPY 的共同美股交易日对齐：`((1 + ETF区间收益) / (1 + SPY区间收益) - 1) × 100`。缺少足够共同交易日时显示 `—`，不以插值或 BTC 周末数据补齐。
- BTC 保留 `crypto-24x7` 日历，不参与美股 RS 对比；其周末/全天候日期只用于 BTC 自身展示，因此“交易日历差异”属于预期信息而非数据错误。

### 验证证据

- Python 数据与 UI 契约测试：`53` 项全部通过。
- JavaScript 语法检查：通过。
- 本地快照已生成真实 RS：例如 XLV `RS1M +0.91% / RS3M +12.60% / RS6M −4.73% / RS12M +5.03%`，COPX `+14.50% / +5.33% / −12.03% / +63.69%`；BTC 的 `relativeStrength` 保持为空。
- 浏览器回归：页面标题与内容正常、控制台 error/warn 为 `0`；板块 `RS1M` 和行业 `RS3M` 排序后 SPY 仍为首行；两张表均可在内部滚动区域访问长期 RS 列；390px 宽度无页面级横向溢出。
- Figma：本轮未创建外部 Figma 文件，因为没有提供可编辑的既有 fileKey；以本地网页预览完成视觉验证，避免生成无法纳入项目的重复设计资产。

### 下一步入口

继续使用现有本地页面与快照，不重复创建 RS 文档或组件。若用户确认数据口径，再进入 RS 数值的异常监控（缺失、极端值、基准日期漂移）或真实数据源替换评估。

## 2026-08-31 Stockbee 半年数据与数值显示（本次完成）

- RS1M/RS3M/RS6M/RS12M 改为显示带正负号的纯数值（例如 `+14.50`），不再附加 `%`；RSI14 保持纯数值格式。
- Stockbee 抓取按最新源日期向前 183 个自然日保留，当前快照为 128 行，日期范围 `2026-02-26 → 2026-08-28`，字段仍为完整 15 列。
- 市场宽度图表与展开表格均读取半年快照；默认折叠状态、顶部横向滚动条、日期固定和内部纵向滚动逻辑保持不变。
- Python 数据与 UI 测试：`56` 项全部通过；JavaScript 语法检查通过。
- 本地浏览器已验证：半年折线范围正确、展开后 128 行、RS 列无百分号、控制台无错误。

## 2026-08-31 RS 定义核对与行业表布局修订

- 已移除行业动能榜右侧重复的 `SIGNAL NOTE / RSI ≥ 60` 提示卡，行业表改为单栏全宽布局，避免右侧空白与指标重复解释。
- 已核对 TradingView 的 RS Rating 脚本与雪球相对强度说明：两者描述的是横截面相对排名（通常 1–99，RSI 不是同一指标），TradingView 还采用约 12 个月、四季度加权的排名思路。
- 当前网页中的 RS1M/RS3M/RS6M/RS12M 仍是“ETF 相对 SPY 的区间超额表现数值”，计算为 `((1 + ETF收益) / (1 + SPY收益) - 1) × 100`，因此与 RPS/RS Rating 百分位存在语义差异；本次不擅自改公式，等待确认后再决定是否切换为 1–99 横截面排名。
- 新增 UI 契约测试，确保 RSI 提示卡不会回归；后续验证需同时检查行业表全宽显示与 RS 口径。
- 验证结果：UI 契约测试 18 项通过，`app.js` 语法检查通过；浏览器检查确认右侧卡片不存在、行业表为单栏全宽，390px 宽度仍可通过表格内部滚动访问数据，控制台无错误。完整数据管线测试本轮另有既存 Windows 本地运行进程超时，未涉及本次 UI 改动。

## 2026-08-31 RPS / RS Rating 试运行

- 按用户确认先试用 RPS/RS Rating 口径：RS1M、RS3M、RS6M、RS12M 改为在各自 ETF 集合内按区间收益横向排名，最高 99、最低 1；SPY 保持固定首行，但使用其真实排名分数。
- 后端新增 `relative_strength_ratings`，按板块集合与 60 个行业 ETF 集合分别计算，并写入 `relativeStrengthRating`；原有相对 SPY 超额表现保留在 `relativeStrength` 供审计与后续比较，不覆盖历史字段。
- 前端新增同口径回退计算，旧快照没有评分字段时也能由本地 K 线即时生成；RS 单元格改为整数显示，70 以上绿色、30 以下红色，其余为中性。
- 页面说明已明确 RS1M/3M/6M/12M 为 1–99 横截面相对强度评分；脚本缓存版本已更新，浏览器可见评分如 SPY `46/39/69/76`、XLE `84/84/61/99`。
- 验证结果：市场数据与 UI 契约测试（排除既存 Windows 本地运行进程测试）共 59 项通过，`app.js` 语法检查通过；浏览器控制台无错误，行业右侧 RSI 卡仍不存在。
- 本版属于试运行口径，后续需确认是否保留整数评分，或进一步实现 TradingView 风格的四季度加权 12 个月 RS Rating。

## 2026-08-31 RS 暂停与表格对齐修订

- 按用户最新要求，暂时从主动网页 UI 移除 RS1M/RS3M/RS6M/RS12M 列、排序入口和 RPS 前端计算，板块与行业表回到 RSI14、Δ1/Δ5/Δ20（行业为 Δ5/20）及状态/权重股字段。
- 保留后端 RS 计算函数与快照字段作为暂停的数据能力，未删除生成逻辑，避免未来重新启用时重做数据契约；当前页面不读取或展示这些字段。
- 表格统一使用固定布局、明确列宽和数字列右对齐，板块表最小宽度调整为 860px、行业表为 760px；表头与数据行的列数及像素宽度已在浏览器核对一致。
- 行业与板块表补充显式 `colgroup` 列定义，锁定表头、SPY 固定行和普通数据行的同一列网格；浏览器实测三者每列起点完全一致。
- 修复 SPY 固定行垂直偏移：固定行 `top` 从 51px 调整为与表头实际高度一致的 40px，滚动行业表时不再从固定行上方或下方露出其他行文字。

## 2026-08-31 指数卡片日内 K 线交互

- 指数与 BTC 卡片默认显示当日小时 K 线：美股指数使用 7 根 1H 模拟柱，BTC 使用 24 根 1H 模拟柱；数据由本地最新日线快照确定性生成，不新增浏览器网络请求。
- 鼠标悬停或键盘聚焦卡片 K 线时切换为近 1 个月日 K 线，移开/失焦后恢复当日 1H；卡片下方同步显示当前视图标签，并提供可访问的 `tabindex` 与 aria 描述。
- 浏览器已验证 5 张卡片正常渲染，默认标签为“当日 · 1H”（BTC 为“当日 · 1H（24H）”）；脚本语法检查和 UI 契约测试均通过。
- 验证结果：UI 契约测试 18 项通过，`app.js` 语法检查通过；浏览器确认 RS 列不存在、行业右侧 RSI 卡不存在、板块 8 列/行业 7 列对齐，390px 下仍由表格内部滚动承载，控制台无错误。

## 2026-08-31 本地执行状态（GitHub 暂停后）

### 当前结论

项目已经从“网页 UI 原型 + 本地行情刷新”进入“本地可审计市场分析管线”阶段。GitHub 远程仓库、推送、PR、Pages 发布和线上授权操作全部暂停；本地代码、分支和运行方式保留。

### 本地分支与版本

- 主工作区：`StockTest/`，`main` 当前为 `5423708`，仅比 `origin/main` 多 1 个本地忽略规则提交；没有继续推送。
- 隔离开发区：`.worktrees/market-analysis-evidence/StockTest/`，本地分支 `feature/market-analysis-evidence`，当前最新为 `6d315f6`。
- 特性分支尚未合并回 `main`，也没有远程跟踪分支；新的本地需求应优先在该隔离分支继续，避免覆盖主工作区。
- `sources/` 未修改，继续保持只读。

### 已完成的本地分析能力

1. **来源与证据契约（Task 1，已审查通过）**
   - `data/source_registry.json` 登记一期允许来源：Yahoo Chart、Binance 公共 K 线、Stockbee、质量报告；后续 SEC/Fed/BLS/BEA/Treasury/ETF 官网来源保持禁用。
   - `data_pipeline/market_analysis.py` 拒绝重复 ID、禁用来源、无证据 claim、lead 驱动评分和空评分证据，并支持原子写入。
   - 任务提交：`a144187`、`e333c67`、`281eeed`；最终定向/完整测试为 59 项通过。

2. **五因子确定性评分（Task 2，已审查通过）**
   - 已实现趋势、宽度、动量、轮动、风险偏好五因子；使用 Wilder RSI、固定阈值、缺失即 `unavailable`。
   - BTC 只有在存在与美股 `asOf` 同日有效 K 线时才进入正式风险偏好评分；周末 BTC 仍可作为页面最新行情展示，但不会倒灌周五结论。
   - Python 与浏览器 RSI 已统一为 Wilder 口径；动量评分固定为 12 个板块/风格 + 60 项行业目录中排除 IBIT 后的 59 个合格观测，共 71 个。
   - IBIT 只参与风险偏好，不参与动量；ARKK 的双业务角色保留，但证据引用稳定去重。
   - 任务提交：`ca81ca1`、`6a75faa`、`5086dc8`；最终完整数据管线测试为 70 项通过。

3. **证据化结论与置信度（Task 3，已审查通过）**
   - 已实现 `build_market_analysis`、evidence materialize、claim/briefing/watchNext 引用校验、置信度和本地 CLI。
   - 质量报告会影响置信度但不改变因子分数：high 每项扣 0.15（上限 0.30），medium 每项扣 0.05（上限 0.15），low 只展示不扣分。
   - provider 使用封闭映射；`binance-spot` 会规范为 `binance-public-klines`，未知或禁用 provider 直接失败。
   - 当前真实复制快照已生成并回读通过：`asOf=2026-08-28`、状态“震荡”、总分 `-1`、置信度 `1.00`、163 条 evidence、10 条 claims。
   - 任务提交：`ba16585`、`9b91b93`；最终完整数据管线测试为 80 项通过。

4. **刷新流程接入（Task 4，实现已完成，独立复审待做）**
   - `refresh_local_data.py` 已在快照写入成功后生成分析，并将分析状态、`asOf`、置信度、生成时间写入刷新状态。
   - 同一 `asOf` 且输入指纹、canonical registry 和现有分析均有效时复用，不重复构建；快照、质量报告、逻辑刷新状态或 registry 改变时重建。
   - builder/writer 失败不会覆盖上一份有效 analysis；刷新状态标记为 `partial`，保留失败原因和最近完整成功时间。
   - 任务提交：`0cd2bcf`；本地定向 7 项、完整数据管线 83 项通过，离线 copied-snapshot smoke 成功。
   - 待办：按 SDD 流程完成 Task 4 独立规格/质量复审；同时清理不应进入版本历史的临时 SDD 报告提交。

### 当前未完成与明确暂停

- **未完成：** Task 4 独立复审；Task 5 首页市场结论卡和证据抽屉；Task 6 端到端浏览器 QA、文档收口和本地交付验收。
- **暂停：** GitHub 推送/上线、GitHub Pages、主题 Top 20 AI 排名、小程序、新闻热度、独立基本面 UI、真实行情 API 直连浏览器。
- **本地服务：** 继续使用 `start-local.cmd`、`status-local.cmd`、`stop-local.cmd`；不要为隔离分支重复启动 8765 端口，若要验证特性分支请使用单独本地端口。

### 新对话启动入口

新窗口必须先阅读 `PROJECT_HANDOFF.md`、`PROJECT_CONTEXT.md` 和本文件最近这一节，然后运行本地测试。建议从以下上下文开始：

> 请只在本机继续 StockTest，不进行任何 GitHub 推送、PR、Pages 发布或授权操作。先阅读 `StockTest/PROJECT_HANDOFF.md`、`PROJECT_CONTEXT.md`、`PROJECT_PROGRESS.md` 最近一节；当前本地特性分支 `.worktrees/market-analysis-evidence/StockTest` 已完成 Task 1–3，Task 4 已实现但待独立复审，Task 5/6 尚未开始。不要重复制作已有页面、数据契约或进度文档；先运行现有 data_pipeline 测试和 `app.js` 语法检查，再等待我提出新的本地需求。主题 AI、小程序、新闻热度、基本面 UI 与线上部署继续暂停。

## 2026-08-31 指数卡片 15 分钟 K 线与悬停气泡

- 按最新需求，SPX、NDX、DJI、RUT 四个美股指数卡片默认改为当日 15 分钟 K 线预览，使用 26 根柱覆盖 09:30–16:00 美股交易时段；BTC 保持 24 根 1 小时 K 线（24H）不变。
- 鼠标移入指数卡片或键盘聚焦 K 线时，不再替换原卡片内容，而是在卡片外显示独立气泡；气泡展示近 1 个月（21 个交易日）日 K 迷你图、区间日期和区间涨跌幅，移开/失焦后关闭。
- 预览仍使用本地快照驱动的确定性模拟日内柱，不新增浏览器网络请求；真实 15 分钟接口接入留待行情数据阶段。
- 为避免缓存旧脚本，页面资源版本更新为 `intraday-bubble1`。
- 验证结果：UI 契约测试 18 项通过，`app.js` 语法检查通过，`git diff --check` 通过；浏览器 1280×720 确认 5 张卡片、4 张 15M + BTC 1H 标签、气泡默认隐藏，悬停后气泡显示且原卡片标签保持不变；控制台无 error/warn。390×844 移动宽度下两列布局无横向溢出。

## 2026-08-31 指数预览折线面积图与放大气泡

- SPX、NDX、DJI、RUT 的当日 15M 预览由蜡烛图改为折线 + 半透明面积图，并在图内增加 09:30、盘中、15:45 的时间刻度；BTC 保持 1H 蜡烛图。
- 悬停/键盘聚焦时显示 320px 宽的独立放大气泡，使用近 1 个月日线折线面积图和日期区间/涨跌幅摘要；气泡会根据卡片位置优先显示在左右侧，空间不足时自动改为上下定位，避免被下方表格裁切。
- 为避免 fixed 气泡受入场动画 transform 影响，卡片与区块入场动画改为仅透明度过渡；气泡滚动时会重新定位。
- 验证结果：UI 契约测试 18 项通过，`app.js` 语法检查通过，`git diff --check` 通过；浏览器确认默认 4 张 15M 折线面积图 + BTC 1H、气泡放大宽度 320px，首张卡片右侧定位、末张卡片左侧定位；390×844 下气泡落在视口内且页面无横向溢出，控制台无 error/warn。

## 2026-08-31 指数卡片放大预览优化

- 将美股指数卡片的日内 15M 预览调整为带面积填充的折线图，图内显示 09:30、盘中与 15:45 时间刻度；BTC 仍保持 1H 蜡烛图。
- 悬停气泡改为 320px 的独立放大面板，展示近一个月日线折线面积图、日期轴与区间涨跌幅；根据卡片左右空间自动选择侧向定位，移动端空间不足时回落到上下定位。
- 气泡使用 fixed 定位并随滚动/窗口变化重新计算，避免被板块表格的滚动容器裁切；保留原卡片不变，键盘聚焦仍可触发。
- 验证结果：UI 契约测试 18 项通过，JavaScript 语法与 diff 检查通过；浏览器 1280×720 确认折线/面积图、SPX 右侧气泡与末卡片左侧气泡均可用，390×844 确认无横向溢出与气泡越界，控制台无 error/warn。

## 2026-08-31 指数卡片分钟级面积图与气泡放大

- 五张指数卡片（SPX、NDX、DJI、RUT、BTC）统一改为当日每分钟折线面积图：美股按 390 个交易分钟覆盖 09:30–15:59，BTC 按 1,440 分钟覆盖 00:00–23:59；此前的 BTC 小时蜡烛图已按最新需求移除。
- 独立悬停/键盘聚焦气泡由 320px 放大到最多 480px（移动端按视口收缩），图表高度调整为 180px，并在标题下显示当前价格。气泡仍使用近 1 个月日线面积图，不改变原卡片内容；滚动、左右空间不足时会自动重新定位。
- 卡片下方标签统一为“当日 · 1M 面积图”，BTC 额外标注“24H”；canvas aria-label 同步为“当日每分钟折线面积图”。
- 资源版本更新为 `minute-bubblezoom1`，避免本地浏览器继续读取旧缓存。
- 验证结果：UI 契约测试 18 项通过，`app.js` 语法检查通过，`git diff --check` 通过；浏览器桌面端确认 5 张卡片、5 张分钟级面积图、气泡宽度 480px、价格字段和独立定位；390×844 确认气泡收缩至 362px 且右边界 376px、页面无横向溢出；控制台 error/warn 均为空。

## 2026-08-31 指数预览改为 10 分钟与 BTC 1 小时 K 线

- 根据可读性反馈，美股指数卡片由 1 分钟预览收敛为 10 分钟折线面积图，每个交易日使用 39 个点（09:30–15:50）；BTC 独立改回 1 小时 K 线，使用 24 根柱覆盖全天。
- 悬停气泡保持上一版 480px 放大规格、当前价格和近一个月日线面积图，不随卡片默认预览类型改变。
- 卡片标签与 aria 描述同步更新为“当日 · 10M 面积图”和“当日 · 1H（24H）K 线”，资源版本更新为 `10m-btc1h`。
- 验证结果：UI 契约测试 18 项通过，`app.js` 语法检查通过，桌面端确认四个美股卡片为 10M 面积图、BTC 为 1H K 线；气泡价格正常显示，移动端无横向溢出，控制台无 error/warn。

## 2026-08-31 真实日内数据接入

- 指数卡片已接入本地真实日内快照：SPX、NDX、DJI、RUT 使用 Yahoo Chart 5 分钟数据在本地聚合为 10 分钟（完整美股交易日 39 根，09:30–15:50）；BTC 使用 Binance 公共 K 线 1 小时数据（UTC，24×7）。浏览器仍只读取 `data/market_snapshot.json`，不直接访问外部 API。
- Yahoo 对这些指数的原生 `10m` 请求返回 HTTP 400，因此没有把失败接口伪装成 10 分钟数据；管线明确保留 5 分钟来源并执行本地 5→10 分钟 OHLCV 聚合，页面消费后的契约为 `intraday.interval = 10m`。
- 最近一次真实刷新时间为 `2026-08-31T08:31:41Z`：日线 76/76 标的加载成功，SPX/NDX/DJI/RUT 各 39 根 10M，BTC 9 根 1H（当天仍在进行，最后 1 根标记 `incomplete`）。美股日内最新会话为 `2026-08-28`，BTC 日内按 UTC 最新日期独立处理。
- 日内质量检查已纳入管线：时间戳唯一且递增、OHLC 边界、成交量非负；质量报告记录点数、首末时间和未收盘根数。最近报告为 0 个 high/medium finding，1 条预期的美股/BTC 交易日历 info note。
- 后台刷新默认包含日内数据，仍由本地每小时刷新进程更新；若日内源失败，日线快照与页面回退逻辑保持可用。
- 验证结果：data pipeline 全量 68 项通过，UI/市场数据定向测试通过，`app.js` 语法检查和 `git diff --check` 通过；浏览器桌面端确认 5 张卡片读取最新本地价格、4 张 10M 面积图 + BTC 1H 标签，悬停气泡宽 480px 且显示价格；390×844 下页面无横向溢出，气泡右边界 376.4px，控制台 error/warn 均为空。

## 2026-08-31 BTC 4 小时预览与高清悬停图

- BTC 日内预览按最新需求从 1H 切换为 Binance 原生 4H K 线；前端默认最多显示 6 根覆盖 24H，当前 UTC 日未完成时只显示已形成的根并保留 `incomplete` 状态。美股 10M 口径不变。
- 悬停气泡中的 Canvas 统一至少以 2× 设备像素比绘制，并设置圆角线帽与连接；CSS 尺寸不变，放大图的实际像素缓冲区为 CSS 尺寸的两倍，改善放大后模糊和锯齿。
- 最近一次真实刷新为 `2026-08-31T09:22:11Z`：SPX/NDX/DJI/RUT 各 39 根 10M，BTC 3 根 4H（00:00、04:00、08:00 UTC，08:00 根未收盘），5 个日内源均加载成功。
- 验证结果：定向数据/UI 测试 33 项通过，全量 data_pipeline 测试 68 项通过；浏览器确认 BTC 标签为“当日 · 4H（24H）K 线”、气泡价格正常、气泡 Canvas 884×360 像素对应 442.4×180 CSS 尺寸；390×844 无横向溢出，控制台 error/warn 为空。

## 2026-08-31 BTC 2 小时预览与气泡清晰度增强

- BTC 日内预览切换为 Binance 原生 2H K 线，前端最多显示 12 根覆盖 24H；美股指数保持 Yahoo 5M 本地聚合的 10M 面积图。
- 悬停气泡最大宽度由 480px 提升至 640px，图表高度提升至 220px；普通图表至少 2×像素缓冲，气泡图表至少 3×像素缓冲，并使用圆角线帽与连接，减少放大后的锯齿和模糊。
- 资源版本更新为 `2h-bubble3x`，避免浏览器缓存旧版脚本与样式。
- 最近一次真实刷新为 `2026-08-31T09:36:29Z`：SPX/NDX/DJI/RUT 各 39 根 10M，BTC 5 根 2H（00:00–08:00 UTC，08:00 根未收盘），5 个日内源均加载成功。
- 质量报告为 76/76 日线标的、0 个 high/medium finding；未收盘的当前 2H 根继续保留 `incomplete`，不人为补齐。全量 data_pipeline 测试 68 项通过，浏览器桌面/390×844 回归无 console error/warn、无横向溢出。

## 2026-08-31 悬停气泡改为 K 线并叠加 EMA

- 悬停气泡的近月图由面积折线改为真实日线蜡烛图，保留开高低收结构与涨跌颜色。
- 新增 EMA9 与 EMA21 计算和绘制；EMA 使用完整日线序列计算后截取最近 21 个交易日，避免线条从窗口首点重新初始化。
- 气泡增加“K 线 / EMA9 / EMA21”图例，标题明确为“近 1 个月日线 K 线”，价格字段保持不变。
- 浏览器验证：气泡宽 640px、Canvas 1794×660（CSS 598.4×220，3×像素缓冲），图例三项可见；390×844 下气泡右边界 379.8px、页面无横向溢出，控制台无 error/warn。

## 2026-08-31 顶栏实时双时钟

- 在“盘后研究简报 / 美股市场”后加入美东与北京时间两个时钟胶囊。
- 使用 `America/New_York` 与 `Asia/Shanghai` 时区格式化，每秒更新，自动适配美国夏令时；时间元素同步写入 ISO `datetime`。
- 浏览器验证：初始时间显示正常，等待 1.2 秒后秒数递进；桌面端顶栏无重叠，390px 下无横向溢出，控制台无 error/warn。

## 2026-08-31 板块 ETF 改为 TradingView 外链

- “板块 ETF 动能”表格中的 ETF 代码改为账号图表模板 `https://tw.tradingview.com/chart/e2o5U28E/`，自动套用 `AMEX:代码` 并在新标签页打开。
- 板块表链接不再带 `data-etf`，因此不会触发右侧详情抽屉；“板块前十大权重股”区域继续保留 `data-etf` 按钮和抽屉逻辑。
- 浏览器验证：首行 SPY 链接为 `AMEX%3ASPY`、目标为 `_blank`，未绑定抽屉钩子；点击 XLV 前十大后右侧抽屉仍正常打开；控制台无 error/warn。

## 2026-08-31 导航改为顶部横向并移除搜索入口

- 左侧产品切换与页面导航整体移至页面顶部，改为横向 sticky 导航；主内容不再保留左侧 240px 偏移。
- 删除顶栏搜索框、“查找 ETF”按钮及 Cmd/Ctrl+K 聚焦逻辑，避免保留不可见或无效入口。
- 美东与北京时间胶囊字号和内边距放大，时间仍每秒更新；窄屏导航改为可横向滚动，390px 页面无横向溢出。
- 浏览器验证：1440px 下导航横向位于最顶、搜索与查找按钮不存在、时钟 13px；390px 下内容宽度 375px、无横向溢出，控制台无 error/warn；全量测试 71 项通过。

## 2026-08-31 美股指数改用 Yahoo 原生 5 分钟数据

- 按最新口径，美股 SPX、NDX、DJI、RUT 不再执行本地 5→10 分钟聚合，直接消费 Yahoo Chart 原生 `5m` 日内柱；前端标签更新为“当日 · 5M 面积图”，BTC 继续保持 Binance 原生 2H K 线。
- 美股前端预览上限调整为 79 根，覆盖 Yahoo 返回的完整 5M 会话（09:30–16:00，含收盘时刻记录）；若 Yahoo 返回较短窗口，页面按实际可用根数绘制并保留日内缺失/未收盘状态，不伪造数据。悬停气泡仍为独立 640px 近月日线 K 线并叠加 EMA9/EMA21。
- `fetch_market_data.py` 的 Yahoo 分支改为 `normalize_yahoo_intraday(... interval_minutes=5)` 直通写入快照；Binance 分支和通用聚合函数/单元测试保留，供其他离线场景使用，但不再参与当前美股刷新路径。
- 文档与资源版本同步为原生 5M：`PROJECT_CONTEXT.md`、`PROJECT_HANDOFF.md`、`PROJECT_ROADMAP.md`、`README.md` 和 `index.html?v=20260831-incomplete`。
- 真实刷新与质量报告已完成（`2026-08-31T11:24:50Z`）：SPX/NDX/DJI/RUT 均为 Yahoo `5m`、各 79 根（13:30–20:00 UTC，完整会话）；BTC 为 Binance `2h`、6 根（当日已形成部分，最后一根 `incomplete`）。快照覆盖 76/76，质量报告 findingCount=0，仅保留预期的交易日历 info note。
- 验证结果：市场数据与 UI 契约定向测试 36 项通过；新增契约断言锁定美股 `5m` 与 BTC `2h`；`app.js` 语法检查与 `git diff --check` 通过。浏览器桌面端确认 5 张卡片、4 张 5M 面积图、BTC 2H K 线、640px 独立气泡/K 线/EMA9/EMA21 与价格；390×844 下 clientWidth=375、scrollWidth=375，控制台 error/warn 均为空。

## 2026-08-31 BTC 未收盘柱显示修正

- 用户反馈 BTC K 线最右侧出现小点。确认该点原本是蜡烛图绘制函数额外添加的末端定位圆点，不代表缺失数据；同时核对快照发现 BTC 最后一根 2H 柱确实可能处于 `status=incomplete`。
- 已移除 K 线图末端圆点，避免把图形标记误认为未生成的柱；未收盘柱本身继续完整绘制，并在卡片说明中显示“未收盘 · 悬停放大近 1 个月”，Canvas 无障碍标签同步增加“含未收盘柱”。
- 浏览器验证：BTC 卡片显示最后一根 K 线与未收盘提示，末端不再出现孤立小点；控制台 error/warn 为 0，页面布局正常。UI 契约测试 21 项通过，JavaScript 语法检查通过。

## 2026-08-31 行业状态列与板块状态对齐

- 行业动能榜新增“状态”数据列，按当日 Δ5 自动映射为“改善 / 盘整 / 转弱”，并沿用统一状态标签样式；SPY 固定首行逻辑不变，前十大权重股入口仍保留。
- 板块 ETF 动能表的状态表头与状态单元格统一居中对齐；行业表新增列宽与原有字段重新归一到 100%，避免状态列因固定表格布局出现漂移。
- 验证结果：UI 契约测试 21 项通过，JavaScript 语法与 `git diff --check` 通过；浏览器确认板块状态表头/单元格左右边界一致（均为 `1123.5–1271.2px`），行业状态列表头/单元格左右边界一致（均为 `1148.1–1271.2px`），控制台 error/warn 为 0。

## 2026-08-31 每日 RSI 两个月历史表与行业 SPY 滚动修正

- “板块 ETF 动能”和“行业动能榜”各增加一个可展开的“每日 RSI · 近 2 个月”面板，默认收起，支持从下拉菜单选择板块或行业 ETF；展开后显示最近 42 个美股交易日的 RSI14 数值和强势/中性/弱势区间，避免把 14/60 个标的同时铺成超宽矩阵。
- 历史 RSI 使用同一份本地日线快照逐日回算 14 日 RSI，按最新日期在上、旧日期在下展示；行情快照加载后会自动更新选择器和表格，暂无数据时保留明确空状态。
- 行业和板块的固定 SPY 行改为整行 sticky，并在滚动容器中动态计算 SPY 行下方的遮挡层，覆盖被固定行压住的半行文字，消除滚动时 SPY 与后续数据之间的漏字/空隙；两张榜单的内部滚动保持不变。
- 新增 UI 契约断言覆盖两个 RSI 面板、42 日口径和历史计算函数；资源版本更新为 `rsi-history3`。
- 验证结果：`app.js` 语法检查通过，UI 契约测试通过；浏览器确认两个选择器分别加载 14/60 个 ETF，板块和行业历史表各显示 42 行，切换行业 ETF 后首行数据更新；行业表滚动后 SPY 行与表头连续、无漏字，控制台 error/warn 为空。

## 2026-08-31 COPX 真实行情补齐与刷新容错

- 核对发现问题来自数据快照而非历史 RSI 表：刷新前 `data/market_snapshot.json` 为 `75/76`，`COPX` 在 `metadata.missing` 中，原因是 Yahoo 连接被远端提前关闭，因此页面只能显示案例回退值，无法生成 COPX 的真实历史 RSI。
- 数据管线新增 Yahoo `query1/query2` 双域名尝试和三次短重试；Binance 451 时自动尝试公开只读备用域名 `data-api.binance.vision`，不使用账号密钥。
- Yahoo 当前交易日的临时日线柱现在会保留为 `pendingBar`，不再因临时 OHLC 边界导致整只 ETF 被判定为缺失；收盘指标仍只使用已确认日线。
- 刷新遇到局部源失败时，会从上一次有效快照保留缺失标的，避免一次网络抖动把已接入的真实数据从页面移除；状态中记录 `retainedSymbols` 以便追溯。
- 已完成一次真实刷新：快照恢复为 `76/76`，`COPX` 使用 Yahoo Chart 日线，499 根已确认日线，最新确认日 `2026-08-28`，当前日柱保留为 `pendingBar`；COPX 行显示 RSI14 `61.1`、Δ5 `−0.2%`、Δ20 `+19.7%`，行业 RSI 历史表可显示 42 行（`2026-08-28 → 2026-07-01`）。
- 数据质量报告：`findingCount=0`，覆盖率 `100%`；当前快照仍按美股/BTC 不同交易日历保留一条预期 info note。
- 验证结果：市场数据、刷新容错和 UI 契约测试共 42 项通过，JavaScript 语法检查与 `git diff --check` 通过；浏览器确认 COPX 选项存在、历史表无空状态、首行真实 RSI 值可见，控制台 error/warn 为空。

## 2026-08-31 每日 RSI 双表并列对比

- 将原本分别位于板块和行业模块下方的两个每日 RSI 面板抽出，合并为独立“每日 RSI 对比”区块；桌面端采用两列等宽布局，减少纵向空白，便于同时查看板块与行业 ETF。
- 两张表保留各自 ETF 选择器、展开/收起和最近 42 个美股交易日数据；展开后表头、行高和滚动区域统一，日期轴一致，适合逐行对照。
- 窄屏（≤780px）自动切换为单列上下排列，避免并列挤压和页面级横向溢出；资源版本更新为 `rsi-compare`。
- 验证结果：桌面端两面板等宽（各约 702px），两张历史表均为 42 行；390px 视口下两面板单列排列，页面宽度 375px、无横向溢出；相关测试 42 项通过，控制台 error/warn 为空。

## 2026-09-01 Stockbee 50 指定工作表接入

- 按用户提供的 Google Sheet 地址读取并验证了工作表 `gid=1499398020`：CSV 导出返回 HTTP 200，内容为日期按列排列的 Stockbee 50 名单，最新可读列为 `2026-08-25`。
- 修正 `fetch_stockbee_momentum.py` 的表头识别：兼容带 “Six Month” 描述行和直接从第二行开始股票代码的两种布局，避免漏掉第一只股票；本次快照完整生成 50 行，首位为 `DAIC`。
- `data/stockbee_momentum.json` 已更新为该工作表的真实快照，`sourceStatus=loaded`、`rowCount=50`、`isStale=false`、`ageDays=7`（抓取时间 `2026-08-31T20:16:15Z`）。网页仍只展示代码和 TradingView 跳转，不混入收盘价等指标。
- README 与交接文档已同步改为该 `gid` 和当前日期口径；后续刷新继续使用同一 CSV 导出地址，若来源超过 30 天会由页面自动标记过期。
- 下一步验证：运行 Stockbee、刷新器与 UI 契约测试，并在本地浏览器确认页面显示 50 行及 `2026-08-25` 来源日期。

## 2026-09-01 文件预览与指数气泡修复

- 定位到页面显示空白的原因：用户打开的是 `file:///.../index.html`，浏览器会阻止页面读取本地 `data/*.json`，而不是 Stockbee 快照本身为空。
- `app.js` 新增文件预览检测：若 `127.0.0.1:8765` 可用，会自动跳转到本地 HTTP 页面以加载真实快照；若服务未启动，则在页面底部显示可点击的启动地址和原因说明。
- 指数卡片悬停气泡层级提升为卡片 `z-index:100`、气泡 `z-index:101`，确保覆盖表格、固定行和其他普通组件；抽屉仍保留独立交互层级。
- 浏览器 HTTP 回归：Stockbee 元数据显示 `来源最新日期 2026-08-25 · 每日更新来源`、表格 50 行；悬停 SPX 气泡显示 `价格 7,711.76`、区间 `2026-07-30 — 2026-08-28`，画布实际像素宽度为 1680（3×清晰度），控制台错误/警告为 0。
- 定向测试 30 项通过，`app.js` 语法检查和 `git diff --check` 通过。浏览器安全策略不允许直接导航到 `file:///` 做自动化验证，因此文件跳转逻辑以代码契约和 HTTP 页面实测验证。

## 2026-09-01 Stockbee TradingView 跳转修复

- 定位到 Stockbee 链接失败的原因：名单中的代码交易所不统一，原逻辑对未知代码统一拼接 `AMEX:`；例如 `DAIC` 实际会解析为 `NASDAQ:DAIC`，使用 `AMEX:DAIC` 会进入“商品不存在”。
- Stockbee 50 现在使用 TradingView 的代码解析页 `https://tw.tradingview.com/symbols/{ticker}/`，由 TradingView 自动解析正确交易所后再进入对应股票页面，不再猜测交易所前缀。板块/行业 ETF 仍沿用已核对的自定义图表链接和交易所映射。
- 浏览器实测：`/symbols/DAIC/` 重定向至 `NASDAQ:DAIC`，`/symbols/RFAI/` 重定向至 `NASDAQ:RFAI`，页面标题和代码均可见；自定义图表布局在未登录时可能受 TradingView 权限限制，但代码详情页本身可访问。

## 2026-09-01 指数盘中价格与气泡浮层修复

- 指数气泡现在会优先显示最新盘中快照价格：美股使用 Yahoo 5 分钟最后一根，BTC 使用 Binance 2 小时最后一根；没有盘中快照时才回退到最近日线价格。
- 气泡内增加“盘中价格每小时刷新”提示，并显示最新盘中时间；本地页面每小时重新读取快照，数据刷新后会恢复当前悬停卡片的气泡，不会因重绘而消失。
- 悬停气泡从卡片节点移动到 `document.body` 作为页面级浮层，保留所属卡片引用用于滚动/缩放定位；这样不会受表格、固定 SPY 行或后续分段控件的层叠上下文影响。
- 浏览器实测：SPX 气泡父节点为 `BODY`、`z-index=101`，显示 `价格 7,686.14 · 16:00` 和“盘中价格每小时刷新”；气泡完整覆盖板块模块控件，控制台 error/warn 为 0。
- JavaScript 语法检查通过，UI 契约测试 26 项通过。

### 气泡锚点二次修正

- 发现页面级浮层改造后，定位函数仍只从卡片 DOM 子节点查找气泡，导致计算不到卡片锚点并回到默认左上角。
- `positionIndexHoverBubble()` 现同时读取卡片保存的气泡引用；窗口滚动、缩放和小时快照重绘均使用同一锚点。脚本版本更新为 `hourly-bubble-anchor`。
- 浏览器回归：悬停 SPX 后气泡父节点为 `BODY`，位置跟随卡片计算（示例 `left=279px, top=361px`），不再固定左上角；控制台 error/warn 为 0。

## 2026-09-01 气泡近月日线包含当日临时柱

- 气泡近一个月 K 线现在由最近确认日线加当前 `pendingBar` 组成；当前交易日即使尚未收盘，也会作为最右侧日线柱显示。
- 临时柱只进入气泡可视化，不参与 RSI、区间收益、MA150 或其他收盘指标计算，保持数据口径不变。
- 气泡范围示例已从 `2026-07-30 — 2026-08-28` 更新为 `2026-08-03 — 2026-08-31`，并显示“盘中价格每小时刷新 · 含未收盘日线”。
- 浏览器实测 SPX：`pendingDate=2026-08-31`、盘中价格 `7,686.14 · 16:00`，控制台 error/warn 为 0；UI 契约测试 26 项、JavaScript 语法检查通过。

## 2026-09-01 项目审计、完成度厘清与后续启动计划

### 本次审计范围

- 检查唯一交接入口、项目上下文、路线图和最近进度，确认没有新建重复的进度/交接文件。
- 检查本地运行状态、行情/Stockbee 快照、数据质量报告、刷新状态、测试文件和工作区改动。
- 通过本地浏览器验证页面非空渲染、指数气泡、Stockbee 50 导航、表格/抽屉交互、TradingView 链接以及桌面端横向边界。

### 当前完成度

| 范围 | 状态 | 结论与证据 |
| --- | --- | --- |
| 页面基础与导航 | 已完成 | 顶部横向导航、总览/板块/行业/市场宽度/Stockbee 50、长期投资空白入口均存在；搜索框和“查找 ETF”已移除。 |
| 主要市场卡片 | 已完成 | SPX、NDX、DJI、RUT、BTC 共 5 张卡片；美股使用 Yahoo 原生 5M 日内数据，BTC 使用 Binance 2H；当前快照含真实价格和日内柱。 |
| 指数悬停气泡 | 已完成 | 气泡迁移到 `document.body`，`z-index=101`，显示近月日线 K 线、EMA9/EMA21、盘中价格和未收盘提示；实测气泡位置贴近卡片且不被板块表格遮挡。 |
| 板块/行业榜 | 已完成 | 板块 14 行、行业 60 行（页面默认强势 15）；MA150 趋势、RSI14、Δ1/Δ5/Δ20、状态筛选、SPY 固定行、前十大抽屉和 TradingView 跳转均保留。 |
| 每日 RSI 对比 | 已完成 | 板块与行业各有 42 个美股交易日（近两个月）历史表，已合并为等宽对比区块。 |
| 市场宽度 | 已完成 | 使用 Stockbee 半年原始结构和 128 行快照，支持指标分组、折线切换和原始表横向滚动。 |
| Stockbee 动能股票 | 已完成 | 用户指定工作表 `gid=1499398020` 已接入，50 个唯一代码、最新日期 `2026-08-25`；代码链接使用 TradingView 自动交易所解析页。 |
| 前十大权重股 | 已完成 | 71 个 ETF 均有真实来源记录；普通股票 ETF 使用 StockAnalysis，IBIT 使用 iShares 官方 CSV，USO 使用 USCF 官方 API；详情抽屉显示来源和持仓日期。 |
| 本地刷新与容错 | 已完成 | 本地网页服务和每小时刷新进程均在运行；局部失败保留上一份有效标的，Yahoo 临时日线进入 `pendingBar`，不污染收盘指标。 |
| 市场分析 Task 1–3 | 已完成 | 来源/证据契约、五因子评分与日期对齐、证据化结论已实现并保留在本地特性分支。 |
| 市场分析 Task 4 | 已完成 | 已独立复审成功、部分失败、完全失败、旧快照保留、分析复用、无效分析和写入失败路径；新增测试通过。 |
| 市场分析 Task 5/6 | 未开始 | 首页市场状态卡/证据抽屉和最终本地交付收口尚未进入实现阶段。 |

### 数据审计结果（本次读取）

- `data/market_snapshot.json`：76 个唯一标的，`required=76 / loaded=76`，美股 75 个、BTC 1 个；`pendingBars=75`，表示美股当前日临时柱被保留，不是缺失。
- 主要市场日内层：SPX/NDX/DJI/RUT 各 79 根 `5m`，BTC 11 根 `2h`；源状态和不完整柱均写入快照元数据。
- `data/market_snapshot_quality.json`：覆盖率 `100%`，`findingCount=0`；唯一提示是美股与 BTC 交易日历不同，属于预期信息，不补造周末 K 线。
- `data/refresh_status.json`：最近一次尝试状态 `ok`，市场 `76/76`，Stockbee 宽度 `128` 行；当前本地刷新器仍在运行，间隔 60 分钟。
- `data/stockbee.json`：最新日期 `2026-08-28`，128 行半年表。
- `data/stockbee_momentum.json`：最新日期 `2026-08-25`，50 行，`isStale=false`、`ageDays=7`。
- ETF 持仓元数据：请求 71 个，成功 71 个，`missing=[]`；IBIT 使用 iShares 官方 holdings CSV，USO 使用 USCF 官方 holdings API。USO 的前十大是期货、国债和掉期头寸，不应按股票 ETF 的“公司股票”标准误判为缺失。

### 验证证据

- 完整测试发现：主项目 `87` 项通过；Task 4 特性分支核心复审 `37` 项通过，特性分支本地运行时 `2` 项通过（包含 Windows 进程探测超时保护）。
- `app.js` JavaScript 语法检查通过；`git diff --check` 通过。
- 本地服务状态：网页 PID `30388`、刷新 PID `28720`，地址 `http://127.0.0.1:8765/index.html`。
- 浏览器 1280×720 实测：页面标题为 `StockTest · 美股盘后雷达`，5 张指数卡片、7 个主要 section、Stockbee 50 表 50 行；控制台 error/warn 为 0；文档宽度等于视口宽度，无页面级横向溢出。
- 浏览器交互实测：指数卡片气泡可见，父节点为 `BODY`、`z-index=101`，显示 `价格 7,686.14 · 16:00`、区间 `2026-08-03 — 2026-08-31` 和“含未收盘日线”；点击 Stockbee 导航后定位到区块并保持 50 行；点击行业“前十大”后抽屉显示真实 SPY 10 行持仓及来源日期。
- 本次实际修改：`fetch_holdings.py`、`fetch_market_data.py`、`test_fetch_holdings.py`、本地运行时进程探测保护；同时刷新 `market_snapshot.json` 并更新本进度/交接说明。页面业务视觉未改动。

### 已知限制与风险边界

1. Yahoo Chart 是免密钥的非官方接口，Binance 是公开只读接口；当前通过本地刷新器使用，浏览器不直连外部服务。免费源仍可能限流或改变返回结构。
2. Stockbee 50 公开工作表最新列为 `2026-08-25`，目前距审计日 7 天，尚未过 30 天阈值；若超过阈值，页面应显示过期，不应继续当成今日名单。
3. TradingView 自定义布局需要用户登录/布局权限；Stockbee 50 使用自动交易所解析页，不能保证每个代码都一定存在，但不会再错误固定为 `AMEX:`。
4. “每小时刷新”是本地快照刷新，不是交易所级实时推送；页面只会在重新读取本地快照时更新。
5. IBIT 与 USO 已不再是缺失项，但二者不是普通股票 ETF：IBIT 以 BTC/现金为持仓，USO 以期货、国债和掉期为主；后续 UI 需要保持“资产类型”语义，不能把这些头寸误标成公司股票。

### 后续唯一执行顺序

1. **刷新异常监控**：关注 Stockbee 50 新鲜度、76 标的覆盖率、pending 柱和来源变更；只做有证据的最小改动。
2. **Task 5 市场状态卡与证据抽屉**：仅在用户确认继续推进市场分析首页后开始，不恢复主题 AI、新闻热度或基本面 UI。
3. **Task 6 最终本地交付**：在 Task 5 完成后做端到端验收、更新 `design-qa.md`，确认本地启动和交接文档可复用。
4. 长期投资内容、主题 Top 20、小程序、GitHub Pages、线上授权继续暂停，除非用户重新确认。

### 下一次启动清单（避免重复工作）

1. 只读 `PROJECT_HANDOFF.md`、`PROJECT_CONTEXT.md`、本节和 `PROJECT_ROADMAP.md`；不要重新盘点历史截图或重复生成进度文件。
2. 运行 `StockTest\\status-local.cmd`，复用现有 `8765` 服务和刷新进程，不重复启动。
3. 运行主项目 87 项测试、`app.js` 语法检查和数据质量校验；若无回归，直接进入刷新异常监控或等待用户确认 Task 5。
4. 只有测试或用户新需求明确要求时才改代码；每次完成后继续追加本文件，不新建同类文档。
## 2026-09-01 Task 4 复审与 ETF 持仓缺口处理完成

- Task 4 独立复审：特性分支核心测试 37 项通过，运行时测试 2 项通过；验证刷新成功、部分失败、完全失败、分析写入失败和旧快照保留路径。Windows 进程探测超时已安全降级，不再阻断状态检查。
- 持仓来源修复：IBIT 改用 iShares 官方 holdings CSV（BTC 与现金行）；USO 改用 USCF 官方 holdings API（源数据 17 行，页面展示前 10 行）。两者不再依赖导致缺口的通用解析器。
- 当前快照校验：行情 76/76、前十大持仓 71/71、缺失列表为空，数据质量 findingCount=0；USO 的期货、国债和掉期是基金真实资产类型，不再误判为股票缺失。
- 本次验证：主项目 data_pipeline 全量测试 88 项通过，快照验证通过，`app.js` 语法检查通过。下一步只进入刷新异常监控和来源回归，不重复制作 Task 4 或持仓页面。

## 2026-09-01 刷新异常监控与来源回归

- 发现并修复刷新器直接写 JSON 的并发读取风险：现在使用同目录临时文件、`fsync` 和原子替换，避免浏览器或校验器读到半截快照。
- 新增原子写入回归测试；主项目 data pipeline 全量测试 88 项通过。
- 快照回归结果：行情 76/76、缺失 0、`findingCount=0`；服务重启后刷新进程和网页服务均为 running。
- 当前校验同时覆盖美股交易日历与 BTC 24×7 数据，不把预期的日历差异判定为错误。
- 下一步：观察一轮小时刷新日志，确认异常时仍保留上一份有效快照；除非出现新异常，不再改动已完成的 Task 4 或 ETF 持仓来源。

## 2026-09-01 来源回归校验增强

- `validate_market_snapshot.py` 新增 ETF 持仓完整性检查：请求数、加载数、缺失列表，以及 IBIT/USO 的 `coverageStatus`。
- 新增缺口回归用例；主项目 data pipeline 全量测试 89 项通过，`app.js` 语法检查通过。
- 实际快照仍为行情 76/76、持仓 71/71、`missing=[]`、`findingCount=0`；最近刷新日志无错误。
- 首次全量运行出现一次本地生命周期测试超时，单独重跑和最终全量均通过，判断为运行环境瞬时竞争而非功能回归。

## 2026-09-01 本地服务状态监控修复

- 修复 Windows 进程命令行查询偶发返回空值时的误报：状态检查现在会降级验证 PID 是否仍存活。
- 实测 8765 端口可访问，状态命令返回 server/refresh 均为 `running`；本地生命周期测试 2 项通过，`app.js` 语法检查通过。
- 保持原有安全边界：仅对已有状态记录的 PID 做降级判断，不改变刷新周期、不新增外部数据源。

## 2026-09-01 阶段 1 监控闭环

- 自动刷新已产生新的成功记录：`2026-09-01T08:57:53Z`，`refresh_status.status=ok`，市场 76/76，Stockbee 127 行，Stockbee 50 为 50 行。
- 运行状态回归：网页服务和刷新服务均为 `running`；刷新日志无错误；快照质量 `findingCount=0`。
- 阶段 1“刷新异常监控与来源回归”完成。下一步进入路线图阶段 5（市场分析首页状态卡与证据抽屉），开始前需确认该产品范围。

## 2026-09-01 Task 5 首轮实现

- 总览页新增“查看全部依据”入口和右侧数据依据抽屉，读取本地 `market_snapshot.json` 与 `refresh_status.json` 元数据。
- 抽屉展示行情覆盖、ETF 持仓覆盖、刷新状态、数据日期和来源边界；支持关闭按钮、遮罩点击和 Escape 键关闭。
- 不改动现有指数卡、板块/行业表格、Stockbee 和市场宽度组件；页面仍不直连外部行情源。
- 验证：HTTP 200，入口与抽屉节点存在；UI 契约/快照质量测试 30 项通过；data pipeline 全量测试 89 项通过；`app.js` 语法检查通过。

## 2026-09-01 AI 分析与新闻接入评估

- 用户确认希望将本地行情、Stockbee、网络新闻与 AI 分析共同呈现。
- 已确认当前环境没有 `OPENAI_API_KEY`，项目也没有现成的新闻来源白名单；因此暂不把未授权新闻或不可验证的 AI 结论写入页面。
- 推荐实现边界：后台刷新器抓取白名单新闻并保存标题、来源、发布时间、URL 与抓取时间；AI 只读取本地快照和新闻快照，输出结论、证据引用、asOf、置信度和失效条件；AI/新闻不可用时保留规则分析和上一份有效结果。
- 待用户提供 AI 服务凭据并确认新闻来源白名单后，再启用外部调用；浏览器继续只读取本地 JSON。

## 2026-09-01 AI/新闻接口预留（暂不启用）

- 新增 `data/analysis_sources.json`：按官方/监管、专业财经媒体、X 官方账号三层分级，当前 `aiEnabled=false`、`newsEnabled=false`，X 白名单为空。
- 新增 `data_pipeline/ai_analysis.py`：提供本地证据 payload 和 provider 注入接口；未传入 provider 时明确抛出禁用异常，不执行任何 API 请求。
- 新增 3 项契约测试；主项目 data pipeline 全量测试 92 项通过，`app.js` 语法检查保持通过。
- 该接口只为后续接入预留，不保存 API Key，不抓取新闻，不改变当前页面和刷新流程。

## 2026-09-01 本地市场分析动态化

- 总览页的市场状态、盘后摘要和五项信号已改为由本地行情快照、板块/行业变化、Stockbee 宽度和刷新状态动态计算。
- 状态计算只使用本地数据，不调用 AI 或网络新闻；每次快照刷新后会重新渲染，数据日期和刷新状态同步更新。
- 保留安全边界：缺失或过期数据会降低风险信号，不把规则推断包装成实时投资建议。
- 验证：data pipeline 全量测试 92 项通过，`app.js` 语法检查通过，首页 HTTP 200。

## 2026-09-01 盘后摘要细化

- 盘后摘要现在动态列出当天板块动能靠前的 2 个 ETF、行业动能靠前的 2 个 ETF。
- Stockbee 摘要根据最新 5 日上涨／下跌比给出“强势 / 中性 / 偏弱”判断，并显示具体比值。
- 所有内容来自本地快照，随刷新重新计算；未引入新闻或 AI 推断。
- 验证：data pipeline 全量测试 92 项通过，`app.js` 语法检查通过。

## 2026-09-01 时钟与 Stockbee 分类列

- 右上角美东时钟新增美国日期，时间仍按秒实时更新；北京时间保持原有显示。
- Stockbee 动能股票表新增“板块”和“行业”列，不加入收盘价、RSI 等额外指标；当前名单源只有股票代码，无法从本地证据确认的分类显示“待映射”，不虚构行业归属。
- 验证：data pipeline 全量测试 92 项通过，`app.js` 语法检查通过。

## 2026-09-01 Stockbee 分类列缓存修复

- 用户反馈浏览器仍显示旧的两列表格；确认是浏览器缓存，而非 HTML 未更新。
- 已将脚本版本参数更新为 `20260901-stockbee-classification`，强制加载包含“板块/行业”列的新渲染逻辑。
- 首页直接请求验证返回 HTTP 200；data pipeline 全量测试 92 项通过，`app.js` 语法检查通过。

## 2026-09-01 Stockbee 表格可见性修复

- 修复四列新增后桌面端被横向滚动遮挡的问题：桌面端四列压缩到同一视口，移动端保留横向滚动。
- 每次名单重新渲染时将滚动位置复位到最左侧；同步更新 CSS/JS 版本参数，避免继续读取旧缓存。
- 验证：页面 HTTP 200，data pipeline 全量测试 92 项通过，`app.js` 语法检查通过。

## 2026-09-02 Stockbee 板块/行业真实分类接入

- 后台刷新器新增公开资料分类核验：普通股票读取 [StockAnalysis 公司资料](https://stockanalysis.com/stocks/)，ETF/加密 ETF 回退读取 [StockAnalysis ETF 资料](https://stockanalysis.com/etf/) 的 Asset Class 与 Category。
- 分类结果随 `data/stockbee_momentum.json` 本地快照保存，并记录 `classificationStatus`、来源和覆盖统计；后续刷新复用已有代码的结果，只对新代码补查，避免每小时重复请求 50 个页面。
- 当前 50 条名单已完成 49 条板块+行业双字段核验，1 条仅返回板块、行业标记为“未找到可核验行业”；页面不再显示“待映射”，也不为缺失资料猜测归属。
- 新增解析、ETF 回退和缓存复用测试；分类专项测试 7 项通过，`app.js` 语法检查通过。

## 2026-09-02 Stockbee 分类中文化

- 页面将公开来源的英文板块、行业名称转换为中文展示，保留数据快照中的英文原值作为来源追溯字段。
- 例如 Technology/Software - Infrastructure 显示为“科技 / 基础设施软件”，Financials/Capital Markets 显示为“金融 / 资本市场”。
- 更新脚本缓存版本，避免浏览器继续使用旧版英文渲染；页面说明同步改为“代码、板块和行业”。

## 2026-09-02 Stockbee 行业列显示修复

- 定位到行业列缺失原因：将 `<td>` 错误复用了全局 `.sub-label`（该类为 `display:block`），导致表格单元格脱离 table 网格并纵向叠加。
- 改为独立的 `stockbee-classification` 单元格样式，保持表格单元格默认布局；板块和行业现在在同一行、各自列内对齐。
- 更新 CSS 版本参数并通过 `app.js` 语法检查和本地 HTTP 资源检查。

## 2026-09-02 刷新频率拆分

- 刷新器改为两种模式：`btc-hourly` 每小时只更新 BTC 行情与盘中数据；`daily-close` 仅在美东工作日 17:00 之后刷新美股、ETF 持仓、Stockbee、RSI 等日更数据。
- 日更模式以美东日期去重；周末不触发美股日更，上一份有效快照会继续保留。BTC 快照更新时只替换 BTC 标的，不覆盖其他资产和持仓数据。
- 页面轮询改为每小时更新行情快照与刷新状态；检测到新的日更成功时间后，才重新加载 Stockbee、RSI 等日更内容。
- 当前刷新状态已验证为 `btc-hourly`，行情 76/76、Stockbee 50 条、分类核验 49/50 均保留；新增 BTC-only 刷新测试通过。

## 2026-09-02 市场宽度原始表头遮挡修复

- 修复“字段浏览”展开后纵向滚动时日期列与两层表头错位的问题：日期表头现在与分组表头一起固定，正文不会覆盖到表头区域。
- 统一分组表头和指标表头的固定高度与 `top` 偏移，避免滚动时出现空隙、漏字或指标列错位。
- 更新 CSS 版本参数；UI 契约测试 27 项、`app.js` 语法检查通过。

## 2026-09-02 顶部导航重构

- 按当前页面实际区块补齐导航：总览、关键市场、板块与权重股、行业动能、Stockbee 股票、每日 RSI、市场宽度，共 7 个入口。
- 导航项改为不可压缩的横向滚动项，窄屏下只滚动导航本身，不再让整条页面横向溢出；保留编号和当前项高亮。
- 为关键市场和每日 RSI 区块补充导航锚点，并增加滚动时自动同步当前导航高亮。
- 更新缓存版本；UI 契约测试 27 项、`app.js` 语法检查通过。

## 2026-09-02 Apple UI skill 重构

- 将 `emilkowalski/skills` 的 Apple 设计 skill 与 `dickwu/apple-design-skill` 安装到项目本地 `.codex/skills/`，不写入全局环境，也不影响其他项目。
- 按两套规范复核并优化现有仪表盘：顶部导航与产品切换器采用轻量半透明材质、清晰的当前项层级和即时按下反馈；导航支持横向滚动、滚动吸附和键盘焦点。
- 统一页面圆角、内容留白与表格/卡片垂直对齐，保留数据密度和原有信息架构；增加系统“减少透明度”“增加对比度”下的可读性降级方案。
- 导航脚本现在同步 `aria-current="location"`，便于键盘和读屏用户确认当前区块；新增 CSS/JS 缓存版本 `20260902-apple-color`。
- 视觉基准稿已生成并用于确定深色蓝灰、冷蓝强调色、系统字体和克制层级方向；未新增行情功能或修改数据逻辑。
- 第二轮细化参考 Apple 官方 HIG 关于“功能层材质与内容层分离”、系统字体层级和可访问性要求，并参考 Apple 官网的克制导航与内容分区节奏；没有复制 Apple 品牌素材或营销文案。
- 本轮将全局色彩令牌切换为 Apple 风格系统中性色：浅色使用 `#f5f5f7 / #ffffff / #007aff`，深色使用 `#0b0b0d / #1c1c1e / #0a84ff`，涨跌与提醒改用语义化绿/红/橙；同时保留高对比度与减少透明度的可读性降级。
- 市场宽度原始表格增加按 Stockbee/Excel 语义复现的单元格底色：仅对“4%上涨”和“4%下跌”两列使用原表格的离散颜色（`#f4cccc`、`#e06666`、`#00ff00`、`#339966`）；上涨数量达到 150/300 分档变绿，下跌数量按 150/300 反向分档，其余列保持普通底色；主要宽度、次要宽度和参考指标之间增加清晰的分组边界线。缓存版本更新为 `20260902-breadth-colors`。
- 数据说明：当前 `stockbee.json` 来自 Google Sheet CSV 导出，CSV 不携带原始单元格填充色；本次依据截图中的颜色语义和快照 schema 分组复现视觉效果，不虚构“读取到 Excel 填充元数据”。
- 页面可读性微调：表格正文由 12px 提升至 14px，表头提升至 12px；指数卡片代码/名称/价格、Stockbee 分类、状态标签和 RSI 标题同步放大，移动端保留横向滚动避免压缩或截断。缓存版本更新为 `20260902-readable-type`。
- 四大指数 + BTC 卡片新增 TradingView 跳转：SPX→`SP:SPX`、NDX→`NASDAQ:NDX`、DJI→`DJ:DJI`、RUT→`TVC:RUT`、BTC→`COINBASE:BTCUSD`；支持鼠标点击以及聚焦后 Enter/空格键操作。TradingView 公共符号页已核对，缓存版本更新为 `20260902-index-tv-links`。
- 验证：`app.js` 语法检查通过，刷新/UI 契约测试 33 项通过，`git diff --check` 通过，本地首页 HTTP 200；内置浏览器连接因系统路径错误不可用，已用本机 Chromium 无头截图完成桌面视觉回归。

## 2026-09-02 数据与资料可靠性审计

- 新增 [DATA_RELIABILITY_AUDIT.md](./DATA_RELIABILITY_AUDIT.md)，对行情、Stockbee 宽度、Stockbee 50、ETF 持仓、质量报告、刷新进程及 AI/新闻资料状态进行只读核验。
- 核验结果：行情 76/76 覆盖且 OHLCV/时间序列检查通过；Stockbee 宽度 127 行、Stockbee 50 为 50 个唯一代码（分类 49 条完整、1 条部分）。
- 审计当时发现的可靠性问题包括：持仓权重单位不统一、少数持仓排名缺口/重复代码/异常权重、质量报告落后于当前快照，以及本地 refresh 进程停止；其中单位、报告同步和进程已在下一节修复，结构异常仍保留为质量告警。
- 现有 AI/新闻/X 来源仍为禁用或 pending，主题目录不应被当作已验证的 AI 研究结论；审计未修改任何行情或持仓数据。
- 验证：JavaScript 语法检查通过，刷新/UI/行情质量/持仓/Stockbee 专项测试 52 项通过，本地首页 HTTP 200；主 Binance 域名当前返回 451，备用域名可访问，实际端点记录修复见下一节。

## 2026-09-02 前十大持仓展示与可靠性修复

- ETF 详情抽屉现在只展示来源可核验的前十大代码和名称，不再展示占比、权重进度条或容易误导的单位；没有可验证记录时显示“持仓待核对”和具体来源原因。
- 持仓解析器统一内部 `weightUnit=percent` 契约，并兼容旧快照迁移；USO 额外记录来源报告的 gross exposure 语义。即使未来恢复比例展示，也不会把 iShares 的 100% 误显示为 1.0%。
- 质量校验新增排名连续性、重复代码、非法权重、单位契约和 MA150 数据不足检查；快照写入后自动同步生成 `market_snapshot_quality.json`，避免证据报告滞后。
- 行情抓取会记录实际成功的 Yahoo/Binance 端点，Binance 主域不可用时质量报告可追溯到备用域名。
- 本轮仍保留来源异常记录：部分 ETF 存在排名缺口、重复代码或旧 `asOf`，这些只进入质量报告，不伪造或补齐为确定事实。

## 2026-09-02 当前收束与休息后交接

### 已完成

- ETF 详情只展示来源可核验的前十名单（排名、代码、名称）；不再展示占比或权重条。
- 缺失或部分持仓会保留“持仓待核对”和来源失败原因；不会用其他 ETF 数据替代。
- 持仓内部单位统一为 `percent`，旧快照可在刷新时迁移；质量报告在快照保存后同步生成。
- 行情抓取记录实际成功的 Yahoo/Binance 端点；本地每小时 BTC 刷新与美东盘后日更进程当前均在运行。

### 已核实与仍保留的问题

- ARKF、ARKW、BLOK、DTCR 的跳号已确认来自源站无代码资产行；解析器现在保留这些行，并在页面以“未提供代码”显示，不再把后续第 11 名挤进前十。
- SLX 的两个 RIO 已确认分别是 `$RIO` 与 `!asx/RIO`；解析器现在保存为 `RIO` 与 `ASX:RIO`，不再误报重复代码。
- 部分 ETF 的 `asOf` 可能偏旧，继续在质量报告中保留日期证据。
- BTC 的 MA150 数据不足告警仍保留在后台报告中，但它不是当前页面需求：BTC 卡片没有展示 MA150；后续如收窄校验范围，应只对板块/行业 ETF 检查该字段。

### 当前证据

- 全量 data pipeline 测试 99 项通过，`app.js` 语法检查通过。
- 当前快照行情 76/76、ETF 持仓 71/71；质量报告与快照总日线数一致（37,526）。
- 质量报告当前仅 1 项 medium finding（BTC MA150 数据不足），无 high/critical finding；前述 ETF 排名和 SLX 代码告警已消除。
- 本地首页 HTTP 200；刷新进程状态为 running，模式为 `btc-hourly`。

### 休息后建议顺序

1. 先读取本节和 `DATA_RELIABILITY_AUDIT.md`，不要重复实现已完成的前十大展示。
2. 不要重复修复已确认的 ETF 排名与 SLX 代码问题；后续只监控来源格式是否再次变化。
3. 若页面仍只需要 ETF 的 MA150，收窄质量检查范围，移除 BTC 的 MA150 误导性告警；除非明确要增加 BTC 趋势列，否则不扩展 BTC UI。

## 2026-09-03 ETF 持仓来源异常修复

- 复核 StockAnalysis 原始持仓页后确认：ARKF、ARKW、BLOK、DTCR 的排名缺口来自无代码资产行（如 ARK Bitcoin ETF Holdco、Other Assets and Liabilities、Keppel DC REIT），不是缺失的股票记录。
- 解析器改为保留无代码行并显示“未提供代码”，不再跳过该行后把第 11 名误当成前十；同时保留 `sourceTicker` 以便追溯。
- SLX 的两个 RIO 已确认分别为 `$RIO` 与 `!asx/RIO`，解析器现在保存为 `RIO` 与 `ASX:RIO`，不再把跨交易所证券误判为重复代码。
- 单独刷新 71 个 ETF 持仓并重算质量报告；当前持仓异常告警已清除，仅剩 BTC MA150 数据不足这一项后台准备性告警。
- 验证：全量 data pipeline 测试 99 项通过，`app.js` 语法检查通过；本地网页与每小时刷新进程已重新启动并保持运行。

## 2026-09-04 GitHub 公开发布与上线交接记录

- 已完成公开发布：仓库 [bravo-189/StockTest](https://github.com/bravo-189/StockTest) 已设为 Public，`main` 与远程同步；最后一笔功能修复提交为 `b44561d`。
- GitHub Pages 已启用并验证成功，线上地址为 [https://bravo-189.github.io/StockTest/](https://bravo-189.github.io/StockTest/)，Pages 使用 workflow 构建，HTTPS 已强制开启。
- 已加入 `.github/workflows/refresh-and-deploy.yml`：支持手动运行和每小时调度；美股按盘后日更，BTC 按小时刷新；生成快照后自动校验并部署 Pages，同时将历史快照保存到 `data-state` 分支。
- 线上验证记录：workflow run `33780804836` 成功；公开 `market_snapshot.json`、`market_snapshot_quality.json`、`refresh_status.json`、`stockbee.json` 和 `stockbee_momentum.json` 均返回 200；最新快照日期为 `2026-09-03`，刷新状态为 `ok`，行情 `loaded=76`、`missing=0`。
- 发布前已扫描 68 个受版本控制文件，未发现 API key、私钥、密码、`.env` 或其他凭据；本机 `.codex/skills/` 目录未上传。
- 当前未完成事项：尚未配置 TightPlayer 自定义域名（Pages API 的 `cname` 仍为 `null`）；下一次需要用户提供完整域名和 DNS 服务商信息。另有 GitHub Actions Node.js 20 弃用提示，属于非阻塞维护项。

## 2026-09-04 TightPlayer 自定义域名配置进度

- 截图确认实际注册域名为 `www.tightplayer.asia`；已将 GitHub Pages 的 `cname` 从 `.com` 切换为 `www.tightplayer.asia`。
- 切换后再次验证：GitHub Pages 状态为 `built`、`cname=www.tightplayer.asia`，但本地、Google DNS 和 HTTPS 查询仍返回 NXDOMAIN；因此域名目前仍不能访问，HTTPS 也未开启。
- 待用户在阿里云 DNS 保存并启用：主机名 `www`、类型 `CNAME`、目标 `bravo-189.github.io`（TTL 使用默认值即可）。若存在同名 A/AAAA/CNAME 冲突记录，需要先清理冲突，并确认 `.asia` 域名的 DNS 服务器已完成委派。
- DNS 生效后，继续核验域名、Pages 状态、HTTPS 强制开启和线上资源路径；不要在 DNS 未生效时反复修改 Pages 配置。

### 2026-09-04 阿里云控制台核验结果

- 用户 Chrome 中的阿里云页面确认 `www` CNAME 已保存、已启用，记录值为 `bravo-189.github.io`，TTL 为 10 分钟。
- 阿里云页面同时提示：当前域名查询到的 NS 地址与云解析系统分配的 NS 地址不一致；页面列出的阿里云分配 NS 为 `dns13.hichina.com` 和 `dns14.hichina.com`。
- 因此当前阻塞点不是 CNAME 内容，而是 `.asia` 域名的权威 DNS 委派尚未指向阿里云，或刚注册/实名认证导致同步延迟。公网 DNS 仍返回 NXDOMAIN。
- 下一步应在阿里云域名注册控制台核对并更新 DNS 服务器委派；如果域名当天注册或刚完成实名认证，先等待至次日再复查，避免反复改动解析记录。

## 2026-09-04 真实快照刷新与变化列可读性修复

- 变化列改用明确的中文日周期标签：板块表为“1日变化 / 5日变化 / 20日变化”，行业表为“5日变化 / 20日变化”；数值继续以正负号开头、百分号置于数值末尾（例如 `+0.5%`、`−1.2%`）。ETF 详情抽屉同步使用“5日变化 / 20日变化”。
- 手动执行一次真实数据刷新并成功完成；当前 `refreshMode=btc-hourly`，表示本次只更新 BTC 盘中数据，完整美股/Stockbee 刷新仍按美东盘后每日执行。
- 刷新后的快照质量：行情 76/76、缺失 0；ETF 持仓 71/71；Stockbee 动能股票 50 行；BTC 2H 最新日期为 `2026-09-04`，保留 1 根未收盘柱；质量报告仅保留已知的 BTC MA150 数据不足提示。
- GitHub-only 更新可行：GitHub Actions 每小时调度，脚本按市场状态选择 BTC 小时刷新或美股盘后日更，不依赖自定义域名；域名只影响访问地址。当前 workflow 仍以手动调度和定时调度为主，代码推送不会自动触发数据刷新。
- 验证：针对变化列的回归用例先失败后通过；全量 data pipeline 测试 101 项通过，`app.js` 语法检查和 `git diff --check` 通过；本地浏览器刷新后表格实际显示新标签和百分比数值，控制台 error/warn 为 0。

## 2026-09-04 RSI 日增榜与周期变化说明

- 新增顶部导航区块“RSI 日增榜”，作为每日 RSI 页面后的独立研究视图；页面筛选板块和行业 ETF 中当日 RSI14 较上一交易日上升至少 6 点的标的，并按增幅降序排列。
- RSI 日增榜只使用已确认的交易日收盘柱计算，不把盘中未收盘柱当作最终日线；若数据不足或无法计算，则不进入榜单，并显示无结果提示。
- 新表包含排名、代码 / TradingView、板块/行业、当前 RSI14、前一日 RSI14、RSI 变化。RSI 变化使用“点”（例如 `+6.2 点`），而板块/行业原有 1日、5日、20日变化仍使用收益百分比（例如 `+4.2%`），两者定义保持清晰。
- 验证：新增回归测试先失败后通过；全量 data pipeline 测试现为 102 项通过，JavaScript 语法检查及差异空白检查通过；本地浏览器已确认导航、表头和无结果状态正常，控制台无 error/warn。

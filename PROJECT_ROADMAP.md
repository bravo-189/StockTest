# StockTest 后续工作路线图

> 本文件只记录“接下来要做什么、为什么做、完成标准是什么”。已完成事实统一记录在 `PROJECT_PROGRESS.md`，跨对话启动规则统一记录在 `PROJECT_HANDOFF.md`。新需求进入后，先判断是否属于本路线图；属于则沿用原文件和原组件，不重复制作。

**最后更新：** 2026-09-01（刷新监控与来源回归完成，下一步 Task 5）
**项目范围：** 仅限 `StockTest/` 本地网页原型与本地数据管线
**默认运行地址：** `http://127.0.0.1:8765/index.html`

## 当前基线

已完成并视为不可重复制作的部分：

- 网页盘后市场雷达 UI、深浅主题、苹果式主题按钮、固定侧边栏和响应式布局；
- 5 张主要市场卡片：SPX、NDX、DJI、RUT、BTC；当前默认预览为美股 Yahoo 原生 5M 折线面积图、BTC 2H K 线；
- 悬停/键盘聚焦的独立近月气泡，最多 640px，含价格、日线 K 线和 EMA9/EMA21；
- 板块 ETF 与权重股同模块、SPY 固定首行、状态筛选、RSI14/Δ5/Δ20 排序、12 个权重股入口；
- 行业“强势 15 / 全部 60”、排序、权重股入口和内部滚动；
- Stockbee 半年原始数据、分组指标折线图和可展开原始表；
- 本地 Yahoo/Binance/Stockbee 快照刷新、数据过期/局部缺失/失败状态；
- 本地市场分析 Task 1–4；Task 4 已完成独立复审。

明确暂停、不自动恢复：

- RS1M/RS3M/RS6M/RS12M 网页列；
- 主题 Top 20 AI 排名及其主动 UI；
- 新闻热度、独立基本面页面、小程序、GitHub Pages、线上授权；
- 浏览器直接访问 Yahoo、Binance 或 Stockbee。

## 后续执行顺序

### 阶段 0：新对话启动检查

**目的：** 确认当前状态，避免回退或重复开发。

执行顺序：

1. 阅读 `PROJECT_HANDOFF.md`、`PROJECT_CONTEXT.md`、`PROJECT_PROGRESS.md` 最后一节和本文件；
2. 检查 `sources/` 未被修改；
3. 运行 data pipeline 测试和 `app.js` 语法检查；
4. 运行 `status-local.cmd`，必要时使用 `start-local.cmd`；
5. 只有用户提出新需求，才进入下面阶段。

**退出标准：** 测试通过、网页可打开、没有重复生成页面或文档。

### 阶段 1：刷新异常监控与数据质量回归（P0）

**目的：** 持续确认每小时刷新、发行方持仓来源和分析产物具备可审计的失败保护。

工作项：

- 复查 `refresh_local_data.py` 的成功、部分失败、完全失败和旧快照保留路径；
- 确认同一 `asOf` 和输入指纹时会复用分析，不重复构建；
- 检查 `market_analysis.json`、`refresh_status.json` 的日期、来源、置信度和错误信息；
- 增加或修正针对异常输入、缺失源和写入失败的测试；
- 不改变网页视觉，不接入新外部服务。

**完成标准：** 监控回归通过；持仓缺失仍为 0；失败时不会覆盖上一份有效分析。已完成。

### 阶段 2：免费行情源与日内数据质量核对（P0，已完成第一轮）

**目的：** 为当前卡片的 5M/2H 预览建立真实数据接入边界。

工作项：

- 评估 Yahoo 免费接口能否稳定提供指数/ETF 5 分钟数据；
- 评估 Binance 公共接口提供 BTC 2 小时 K 线的时间戳、时区和收盘状态；
- 将美股交易时段与 BTC 24×7 日历分开处理；
- 保留 Yahoo 尚未收盘的日线到 `pendingBars`，不得混入收盘指标；
- 对每个标的记录来源、抓取时间、数据日期、点数、缺失和异常值；
- 真实数据不可用时，继续使用当前本地确定性预览，不让页面报错。

**完成标准：** 已形成源质量报告；确认 Yahoo 原生 5M、Binance 2H 可用；数据源失败时页面仍可用；浏览器不直连外部 API。

### 阶段 3：本地日内快照接入（P1，已完成第一轮）

**前置条件：** 阶段 2 证明免费源的稳定性和许可边界。

工作项：

- 扩展 `market_snapshot.json` 的 instrument 结构，保留日线和日内 bars 两层；
- 美股卡片读取当日 5M 数据，BTC 读取当日 2H 数据；
- 缺少日内数据时显示明确的“日内数据缺失”状态，并回退到本地预览；
- 气泡继续读取近一个月日线 K 线并叠加 EMA9/EMA21，不因日内接口改变而重做组件；
- 每小时刷新只更新本地快照，页面继续只读取本地 JSON。

**完成标准：** 已完成本地快照结构、每小时刷新和 UI 回退逻辑；76 个标的日线逻辑不回归；阶段 4 第一轮浏览器回归已确认 5 张卡片真实价格、5M/2H 标签与响应式边界。

### 阶段 4：端到端数据与 UI QA（P1，已完成第一轮）

**工作项：**

- 1440×1024、1280×720、1024×768、768px、390px 回归；
- 验证导航、SPY 固定、板块排序、状态筛选、行业 15/60、抽屉、Stockbee 展开；
- 验证 5M 面积图、BTC 2H K 线、640px 气泡、价格显示和键盘操作；
- 验证加载、过期、局部缺失三种可见状态；
- 更新 `design-qa.md`，最终结果必须为 `final result: passed`；
- 只在验证失败时做最小修复，不做无关重构。

**完成标准：** 第一轮已通过：UI 契约、数据测试、语法检查、桌面/390px 浏览器控制台检查全部通过；后续仅在新增数据源或 UI 变更时回归。

### 阶段 5：市场分析首页证据化（P2，首轮已实现）

**前置条件：** 阶段 1–4 已完成；本轮已完成首个可用版本。

工作项：

- 完成 Task 5 市场状态卡和证据抽屉；
- 完成 Task 6 端到端 QA 和本地交付收口；
- 结论必须引用本地 evidence，不把示例值写成实时结论；
- 继续保留主题 AI、新闻热度和基本面 UI 暂停状态。

**完成标准：** 分析结论、来源、asOf、置信度和失效条件可追溯；本地交付文档齐全。

首轮完成内容：总览页“查看全部依据”入口、右侧数据依据抽屉、来源/日期/覆盖率/刷新状态展示，以及遮罩和 Escape 关闭交互。下一步仅补充市场分析结论数据（若用户继续确认），不重复制作抽屉结构。

### 阶段 6：长期投资页面（P3，另行确认）

- 目前只保留 `long-term.html` 空白入口；
- 等盘后雷达稳定并由用户确认后，再设计长期投资数据结构和页面内容；
- 不将长期投资页面内容混入当前盘后雷达。

### 阶段 7：主题 AI、小程序与发布（暂停）

只有用户重新确认后才启动：

- 主题 Top 20：调用研究 agent，保存证据、排名、催化剂和风险；
- 小程序：复用 `DashboardData`，重新设计移动交互；
- GitHub Pages：在本地验收通过后再讨论发布和授权。

## 每次交付的固定报告格式

每个阶段完成后，交付内容必须包含：

1. 本次实际修改的文件；
2. 未修改且明确保留的模块；
3. 测试命令与实际结果；
4. 浏览器尺寸、交互和控制台验证结果；
5. 数据来源、数据日期、缺失和过期说明；
6. 下一阶段是否需要用户确认。

## 防重复与停止条件

- 先查 `PROJECT_HANDOFF.md` 和本文件，再决定是否写代码；
- 同类记录只更新已有文档，不新建第二份进度或交接文档；
- 不修改 `sources/`，不覆盖用户已有本地改动；
- 不因为“看起来可以”就接入新的付费或需要密钥的数据源；
- 数据口径不明确、来源不稳定或测试失败时，暂停下一阶段并汇报；
- 当前首个待执行目标是“阶段 5：市场分析首页证据化”；阶段 1–4 的数据、刷新和第一轮浏览器回归已完成。

## 新窗口可直接使用的启动语句

> 当前口径覆盖：美股已切换为 Yahoo 原生 5M 面积图，BTC 保持 2H K 线，悬停气泡最大宽度 640px、图表 3×像素缓冲。

```text
请只在本机继续 StockTest，不进行 GitHub 推送、PR、Pages 发布或线上授权。先阅读 StockTest/PROJECT_HANDOFF.md、PROJECT_CONTEXT.md、PROJECT_PROGRESS.md 最后一节和 PROJECT_ROADMAP.md。当前网页指数卡片口径是美股 Yahoo 原生 5M 面积图、BTC 2H K 线，悬停气泡最多 640px 且显示价格；RS、主题 AI、小程序、新闻热度和基本面 UI 继续暂停。不要重复制作已有页面、数据契约或文档。先运行现有 data_pipeline 测试和 app.js 语法检查，再从 PROJECT_ROADMAP.md 的阶段 1（Task 4 独立复审）开始，等待我确认是否进入下一阶段。
```

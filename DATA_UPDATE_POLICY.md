# 数据更新策略

当前项目采用两种互不混用的更新路径：

| 数据 | 更新时机 | 发布条件 |
| --- | --- | --- |
| BTC 日线、日内价格和卡片数据 | 每 2 小时（GitHub Actions `5 */2 * * *`） | 只替换 BTC，保留最近一次完整美股/Stockbee 快照 |
| 美股指数、板块 ETF、行业 ETF、ETF 前十大持仓、市场宽度、Stockbee 50、RSI 榜单 | 每个美东交易日收盘后约 3 小时；次日开盘前补刷窗口兜底 | 先确认所有美股标的已到当日收盘；来源延迟时自动重试 6 次，每次间隔 5 分钟 |

盘后日更从美东 19:00 之后开始，并使用最近一个已确认的 NYSE 交易日作为日期。若来源在盘后仍未发布完整收盘价，任务会保留上一份完整快照；次日美东 07:00–12:00 的调度会重新尝试上一交易日，避免因来源延迟导致整天停留在旧日期。周末或节假日不会生成不存在的美股收盘数据；若周五漏刷，会在周末继续尝试补齐最近交易日。

BTC 两小时任务只更新 BTC 相关字段，不代表美股日线、Stockbee 或市场宽度已经完成更新。页面状态会分别显示“BTC 已更新”和“美股与 Stockbee 完整数据”的最后成功时间；应以 `lastFullSuccessAt` 作为完整日更的判断依据。

每次刷新均会：

1. 读取上一份已发布快照作为回退基线；
2. 通过市场质量校验后，才写入 `data-state`；
3. 将已校验的 JSON 同步到 `main`，触发 Vercel 更新；
4. 在 `data/refresh_status.json` 和 `data/market_snapshot_quality.json` 中留下时间、来源覆盖和异常证据。

如果盘后任务因来源暂时不可用而失败，可在 GitHub Actions 的手动运行菜单勾选“强制补刷最近一个美股交易日”。该选项只执行一次完整日更，不会改变平时的两小时 BTC 调度。

持仓源若短暂少返回一个 ETF，只有在上一份记录包含至少 10 行可验证前十大持仓时才会按代码补回，并记录 `retainedSymbols`。无法验证的缺口仍会阻止发布，不会静默显示不完整名单。

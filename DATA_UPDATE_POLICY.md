# 数据更新策略

当前项目采用两种互不混用的更新路径：

| 数据 | 更新时机 | 发布条件 |
| --- | --- | --- |
| BTC 日线、日内价格和卡片数据 | 每 2 小时（GitHub Actions `5 */2 * * *`） | 只替换 BTC，保留最近一次完整美股/Stockbee 快照 |
| 美股指数、板块 ETF、行业 ETF、ETF 前十大持仓、市场宽度、Stockbee 50、RSI 榜单 | 每个美东交易日收盘后 | 先确认所有美股标的已到当日收盘；盘后来源延迟时自动重试 3 次 |

盘后日更只在美东 17:00 之后执行，并使用最近一个已确认的 NYSE 交易日作为日期。周末或节假日不会生成不存在的美股收盘数据；若周五漏刷，会在周末继续尝试补齐最近交易日。

每次刷新均会：

1. 读取上一份已发布快照作为回退基线；
2. 通过市场质量校验后，才写入 `data-state`；
3. 将已校验的 JSON 同步到 `main`，触发 Vercel 更新；
4. 在 `data/refresh_status.json` 和 `data/market_snapshot_quality.json` 中留下时间、来源覆盖和异常证据。

持仓源若短暂少返回一个 ETF，只有在上一份记录包含至少 10 行可验证前十大持仓时才会按代码补回，并记录 `retainedSymbols`。无法验证的缺口仍会阻止发布，不会静默显示不完整名单。

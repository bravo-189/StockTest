---
name: StockTest research terminal
source: VoltAgent/awesome-design-md · linear.app reference
---

# StockTest 设计规范

StockTest 使用 Linear 参考规范中的“近黑画布 + 分层表面 + 单一冷色强调”作为信息终端基线，再保留美股研究场景必须的上涨绿、下跌红和警告琥珀色。它是研究工具，不复制 Linear 的品牌资产或页面结构。

## 视觉令牌

- 深色画布：深蓝黑，不使用纯黑；面板通过 3–4 级表面色阶区分层级。
- 浅色画布：冷白背景、低对比灰线；深浅主题必须保持同一信息层级。
- 强调色：蓝色用于选中、焦点和主操作；绿／红只表达涨跌；琥珀色表达中性或过期。
- 边框：1px hairline；阴影只用于抽屉和浮层，表格与普通卡片不使用厚重阴影。
- 圆角：表格容器 12px、控件 8px、状态胶囊 999px。

## 排版与间距

- 采用 8px 间距体系；区块间距 32–48px，表格单元格内边距 12–14px。
- 正文 14–16px，辅助文字 11–12px；价格、涨跌幅、RSI 和排名使用等宽数字。
- 标题使用紧凑字距与中等字重，避免营销式超大标题挤压研究内容。

## 组件规则

- 表格是主角：优先使用行、分隔线和轻微悬停，不把每一行包装成独立卡片。
- 板块表固定将 SPY 置于首行并标记“锁定”；RSI14、Δ5、Δ20 的表头为可操作排序按钮。
- 行业表用“强势 15 / 全部 60”切换控制可见行数；RSI14、Δ5、20日表头沿用同一排序交互。
- 每个行业行保留“前十大”动作入口，直接打开与板块共用的 ETF 详情抽屉。
- 板块和行业表使用独立内部滚动容器；表头与首行 SPY 使用 sticky 定位，滚动时保持比较基准。
- 状态列使用带文字的只读标签；表头提供紧凑、可键盘操作的筛选菜单，不依赖颜色单独表达状态，SPY 状态不可编辑。
- ETF 权重股使用同一研究区块下的 12 个入口卡，点击后复用右侧详情抽屉。
- 迷你图使用细线和少量填充，避免发光、渐变和大面积红绿背景。
- 主要市场卡片允许在四大指数旁加入 BTC 等跨资产基准；使用相同的价格、涨跌和迷你走势组件。
- 所有可操作元素具备可见焦点、中文标签和至少 40px 的触控高度。

## 响应式

- 1440px：侧栏 + 主内容双栏；权重股入口 6 列、两行展示 12 个 ETF。
- 1024px：压缩主内容间距，表格保留横向滚动；不隐藏核心排序与状态操作。
- 768px 以下：侧栏折叠为顶部导航，入口卡降为 2 列；表格在自身容器内滚动。
- 390px：单列布局，抽屉占满视口宽度，任何数据操作仍可通过键盘或触控到达。

## 本次改造范围

- 主题模块继续暂停，不在主动页面加载或展示。
- 板块 ETF 动能表新增固定 SPY、RSI14／Δ5／Δ20 排序和表头状态筛选。
- 板块前十大权重股入口由 6 个扩展至 12 个；权重明细仍在同一详情抽屉查看。
